profile_overlay_version: 1
agent_id: erhua
managed:
  model:
    default: gpt-5.5
    provider: custom:livecool.net
    base_url: ""
  custom_provider:
    name: Livecool.net
    base_url: https://livecool.net/v1
    model: gpt-5.5
    key_env: LIVECOOL_API_KEY
    api_mode: chat_completions

# This is a field-limited overlay, not a complete Hermes config. The credential is
# migrated into the runtime-local Erhua .env by the deploy runner and never enters Git.

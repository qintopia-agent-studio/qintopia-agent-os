id: agents/xiaoman
type: agent
name: Xiaoman
owner: TBD
status: adopting
risk_level: medium
description:
  Activity and community-event operations Agent that creates governed work items from
  structured signals and human-confirmed inputs.
source:
  path: /home/ubuntu/.hermes/profiles/xiaoman
  reference: runtime-state-2026-07-03-read-only
  disposition: template
production_boundary:
  external_sends: true
  database_writes: true
  runtime_profile: true
  secrets: true
runtime:
  restart_target: hermes-xiaoman
  systemd_user_service: hermes-gateway-xiaoman.service
validation:
  commands:
    - pnpm smoke:sidecar
    - pnpm registry:check
    - pnpm policy:check
    - pnpm agents:profile-bundles:check
dependencies:
  - workflows/activity-promotion
  - runtime/hermes
  - runtime/sidecar
tags:
  - agent
  - activity-ops
  - hermes-profile
next_actions:
  - Provision the four declared profile bundle values through an owner-approved
    server-local configuration path, then record byte-for-byte SOUL and profile parity.
  - Add live profile symlinks only in a later PR with first-cutover backup and rollback
    evidence; keep config, webhook, channel, cron, secrets, and state runtime-local.
  - Keep the production workflow on internal send-ready until Huabaosi generation and
    QiWe delivery pass isolated owner-approved staging evidence.

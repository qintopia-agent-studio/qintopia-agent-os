# Restricted Parser Primitive Registry

This directory contains append-only QiWe `*.primitive.json` parser recipes. The fixed
schema and automatic-release boundary are documented in
`docs/engineering/qiwe-restricted-parser-primitives.md`.

Do not place executable source, credentials, routing fields, or production callback
payloads here.

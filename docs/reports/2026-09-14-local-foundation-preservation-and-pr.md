# 本地人员基础与工作台成果保存

日期：2026-09-14。范围：负责人授权保存本地代码、推送并创建 PR；暂停合并与生产变更仍有效。

## 保存范围与恢复过程

主工作区原分支为 `codex/org-person-workbench-a`，起点为
`2815ab46f2cc0efb3e34cda56f6242bb9af5abf0`。现存未提交成果包含统一人员与欢迎核心、组织与授权工作台、七个既有迁移、注册/本地入口适配、设计及验收文档。

原深色工作台独立目录 `/private/tmp/agentos-org-workbench-confirmed-20260911`
本次已不存在，其 Git
worktree 记录仍在。没有执行清理、pull、reset、远端合并或服务器目录覆盖。

从原工作台任务保存的成功源码补丁恢复限定文件，未执行历史 shell 命令，未复制任务原始记录、凭证或运行数据。恢复前主工作区成果已进入 Git 索引，保留原始版本与恢复增量的分界。恢复后的正式源码位于本仓库
`runtime/sidecar/src/person_collaboration/`，不再依赖临时工作区。

原工作台的 [9 月 11 日交付报告](2026-09-11-organization-agent-workbench-confirmed.md)、
[9 月 12 日覆盖与接入报告](2026-09-12-workbench-coverage-and-c-handoff-merge-hold.md)
同时恢复。它们保留历史路径与当时验收结果，不代表原测试进程、数据库或截图已重建。

## 提交检查与证据边界

原工作台曾记录 21 项协作专项、724 项常规测试通过及 47 项按门禁忽略。上述是历史结果，本轮不冒称重新执行。

本轮先通过敏感信息检查，修正待提交文档的格式与行长。现有简化版 `AGENTS.md`
缺少部署检查要求的原有约束，补回兼容索引；没有删除检查或放宽业务/生产门禁。

本机最初没有 Rust 工具链，提交钩子因此失败；已从 Rust 官方站点安装项目规定的 1.96.0 工具链。工具链保留在已有忽略目录
`.workbuddy`，不提交第三方工具，不更改系统 PATH。

本轮核验结果：

- 首次保存提交
  `41cba2c`，包含 84 个文件；原有 pre-commit 和 commit-msg 钩子通过，未跳过钩子。
- 新版工作台 14 个交付文件全部与原记录的 SHA256 一致，见[恢复清单](evidence/2026-09-14-workbench-source-recovery.json)。格式上下文差异已按原格式化顺序处理，没有凭猜测重写实现。
- Prettier、Markdown、注册表、工作流、部署契约、Xiaoman preflight、Rust
  fmt、前端 JS 语法和启动脚本语法检查通过；敏感信息检查通过。
- `pnpm check:pr:heavy`
  已尝试，但未完整通过。系统 Python 缓存目录最初受限，改用本地缓存后继续；既有 QiWe
  staging
  smoke 随后在系统 Python 下报告临时输出目录非空。使用应用自带 Python 重跑该专项通过，未修改测试或部署脚本。
- 单独尝试
  `cargo test --offline --all-features`，因本机缺少 crates.io 依赖缓存而未进入编译，首个缺项为 anyhow。Rust 全套、Postgres 与 CI 最终结果仍待确认；本轮没有运行数据库迁移或真实发送。

本次 PR 保存成果并供审阅，保留草稿及暂停合并状态。历史测试与恢复哈希说明实现来源，不代替当前 CI 和后续整合验收。

## 已知限制与后续责任

- 工作台群停用预览文案与实际撤权范围不完全一致，见 9 月 12 日报告 D1。本次保存既有成果，不借恢复源码扩展开发或改变业务规则。
- 真实登录、渠道映射、智能体实际消费配置、完整欢迎及生产接收器仍未完成。合成入口不能作为生产登录或发送入口。
- 同事的运行时解耦与测试框架尚待固定提交及接口交接。本 PR 不引入其最新代码，不把创建 PR 当作已整合。
- 七个迁移是已有未提交实现，本轮没有新增迁移内容，也没有应用到任何数据库。
- 创建 PR 后保持待审阅状态，不合并、发布、部署、修改 Base/Workflow/Hermes 或发送业务消息。

恢复方式：保留本 PR 的分支与提交，可在新的独立工作区检出；不要只保存清单而删除唯一源码。后续整合应先取得同事固定版本，在独立整合分支比较和验证。

# 欢迎本地补齐：群确认、首次建档、跨入口识别、分页与电话核对

## 当前范围

本报告对应 `codex/welcome-identity-audit`，基线 `cddcaaa`，工作树
`.worktrees/pr720-ci-reconcile`。仅本地模拟开发，未触碰根工作区、CI、部署规则、生产数据或真实发送。详细接口见[补齐契约](../plans/active/welcome-local-completion.md)。

用户最终规则为：新申请手机号必填、手机号主要匹配、姓名昵称辅助；历史数据不追补。中途提出的证件方案已被用户撤回，没有新增证件字段、算法或迁移。

| 本地范围         | 当前结果                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------------- |
| 申请最小来源投影 | 独立宿主token；完整白名单字段重算004摘要；绑定/版本/许可校验；24小时后不可匹配，不宣称物理删除             |
| 首次人员建档     | 复用原welcome的pending来源建档函数；当前WelcomeSubject明确确认，工作账号不伪造自然人；并发与同键不重复建人 |
| 群呈现与确认     | 已实现prepare/claim/receipt/status/callback；认证持久消息明确引用事项和候选；页面共用同一收据              |
| 发送恢复         | 明确本地模拟传输；先持久claim，未知不重发，只读回原回执；来源/配置/候选版本变化拒绝旧呈现                  |
| 二花识别         | 有完整收据依据的工作账号确认链接，可在普通群聊与私聊复用；撤销失效；PMS gateway_actor仍保留原门禁          |
| UI和历史         | 岗位内待确认/历史记录/资料核对/协作安排标签；事项、账号和收据服务端分页，姓名搜索；未知写请求保留原操作键  |
| 手机号主要匹配   | 本地服务及固定群宿主已补齐，8个新增PG场景通过；岸岸真实HTTP宿主联合模拟待最终共同版本验证                  |
| 真正上线         | 未进行正式renderer迁移、真实微信群发送、生产Profile登记或部署，不能把本地通过描述成线上闭环完成            |

建档只确认所选来源，申请↔住宿、账号↔人员、内容分别保持明确效果。原完整确认可在一次提交中办理已有人员的多个效果；没有表达的效果不会自动补齐。旧收据缺少具体人员/账号效果时不自动升级成二花个人会话证明。任职失效不删除独立已确认身份，但具体操作仍逐次核验当前授权。

## 验证记录

本切片真实业务代码经过以下验证，全部使用本线51847：

- `completion-pg.log`：欢迎确认模块26项通过，含建档并发/重放、旧群呈现失效、来源过期/撤回与中文null摘要契约。
- `completion-common.log`：共同业务108项通过，其中包含上述26项，不重复累计。
- `completion-welcome.log`：原欢迎流程17项通过，含旧首次建档路径和发布门禁回归。
- `completion-python.log`：Person Foundation 11项通过，包括明确模拟传输及未知不重发。
- `completion-harness.log`：测试目录运行器8项通过；新增Rust场景逐个登记完整名称和
  `--exact`。
- `completion-clippy.log`：所有target/feature的Clippy以 `-D warnings` 通过。
- `completion-build.log`：默认Sidecar构建通过。format、Markdown、collaboration检查通过。

首次真实socket专项失败属于测试连接使用错误：探测后残留空连接挡住串行服务，非授权门禁失效。复用首次连接发送实际请求后，普通模型token、错误profile/gateway拒绝，正确host请求通过；后续26项与108项回归均包含此用例。

`pnpm check:pr:auto` 首轮在包检查发现本次Python测试生成的本地
`__pycache__`，未提交缓存。删除该可再生缓存，并设置 `PYTHONDONTWRITEBYTECODE=1`
后按原检查流程重跑；没有修改CI或放宽规则。最终 auto 退出1：light、默认测试851通过/2忽略、all-feature测试866通过/174忽略、Clippy及PG共同业务120项/原欢迎24项通过；随后原
`operations-control-plane-apply-smoke.sh` 拒绝临时库51847，报
`database URL hash is not in the reviewed allowlist`，下游空输出引发JSON解析错误。因此总检查未通过，不能以专项通过代替。未修改部署smoke、白名单或CI；该环境边界须在原受支持部署验证环境执行。

## 迁移与恢复证据

005仅新增已协调的来源投影、群呈现两表及索引，已在51847真实应用。
`qintopia_messages._sqlx_migrations`
的001—005均为success，001—004没有源码变更。005的SQLx SHA384为：

```text
252fa06a895d81188b00ec88d9df2986c630f84baa3585f8b941612716aff758212c56292f3fedd7b8a26d73b8563b01
```

冻结后不再改005。回滚先停本地宿主，保留投影、呈现、收据与原claim；UNKNOWN不可重发。确认撤销只撤销本事项拥有的来源证据，Person和历史保留。旧库51846与用户旧18877未改动。

## 浏览器复查

第一切片609dc20的本任务18881默认构建经过浏览器检查：组织岗位框架保留，岗位内四个标签可切换；历史记录能展开具名确认/撤销收据和记录分页；资料核对无当前投影时明确提示先读取来源；协作安排保留办理群、具名授权人和截止时间。没有新增全局欢迎分类。

原模拟验收授权已到期，页面正确拒绝。仅延长本任务既有非撤销模拟授权以继续检查，没有更改生产授权。浏览器本轮未重走首次建档/多页长列表的每一项操作；相应持久行为由上述PG场景验证，真实使用者最终验收仍待进行。浏览器页面宽度与内容宽度均1581，无横向溢出；本轮控制台error为空。

## 手机号证据职责

总指挥已将固定PMS只读宿主接线交岸岸，本线负责匹配与当前性校验。使用004既有申请
`anan_work_id`
为锚，在原WorkItem独立metadata键保存读取token/版本及脱敏比较结果，遍历受控同范围候选池后按手机号排序，不依赖先有Person或人工先选某个case。

真实依据须来自固定受权 `GET /orders/:id` 的逐active
occupant电话，不能用主住客电话替代同行人。完整号码不进入模型、群消息或metadata。新GET版本先于事件投影时等待同步；更正为null不能回退旧值。共享电话、多人、缺失与冲突仍需有权确认。DTO已由本线与岸岸经总指挥统一冻结，不新增PMS接口。

岸岸拥有申请插件及 `script_action_bridge.py::run_application_one`
的技术未确认恢复薄接线；本线不并行修改这些文件。本报告区分本地服务已验证与宿主联合模拟待验证，不作为生产欢迎流程完成声明。

## 手机号增量与共同基线

第一切片609dc20已冻结，正常整合岸岸0756a8e得到aef39415；接口在f7e47b5冻结。共同分派、store及测试登记并存，迁移001—005无改动。新服务复用申请WorkItem私有metadata键，不新增迁移、权限、模型工具、CI或部署步骤。原PMS业务授权未放宽。

新增真实PG服务验证覆盖完整池超过20、电话优先、共享号码、完整active
occupant集合、逐人null更正不回退、严格DTO、读取失败、版本不同等待同步、旧token拒绝和同token重放。群确认验证相同稳定依据刷新后原消息一次成功、真实变化拒绝、独立人工依据可用、已成功重放不重复、原消息绑定及到期授权拒绝。电话缓存失效不撤销已确认身份。私有metadata不存明文电话，公共WorkItem状态和候选响应不暴露读取token/盐/内部比较证据。

修正前结果：contacts-welcome-pg.log为33/33，包含7个新增电话场景及原26个欢迎确认场景；contacts-python.log为13/13，contacts-pms-python.log为41/41，contacts-bridge.log为18/18。这里的41项是整合阶段5的PMS包，不包括岸岸尚待整合的电话宿主用例；不以模拟broker替代最终真实HTTP→Unix→PG联合验证。最初电话到期测试用PG文本注入非RFC3339日期而失败，改用JSON序列化UTC时间后通过，没有更改过期判断。

原 `contacts-pr-auto.log`
的auto以f7e47b5为基线，在最后边界收紧前执行：light通过，默认测试851通过/2忽略，all-feature测试866通过/187忽略，两种Clippy通过，共同PG133项与resident_welcome
24项通过。最终exit1，原部署apply smoke仍因PG51847的URL hash不在reviewed
allowlist拒绝，随后空输出引发JSON解析错误。没有改CI/白名单，也没有把总检查计为通过。

该轮结束后，自查收紧了“已有独立关系”判断：复用原identity_relations_valid，申请引用来自已验证的当前事项；必须精确匹配本申请、本入住人、有效来源、身份版本和范围。不能仅凭同人及非空旧申请跳过复验。另在群呈现中显示本入住人的电话一致/不同/缺失等理由，变化后新呈现有新引用及具体说明。

最终 `contacts-final-pg.log`
为34/34（8个电话场景已包含），补验同人另一申请、已撤销来源、错误occupant和失效身份版本不能豁免；没有本消息新鲜读取时callback拒绝。同一申请有效既有关系、独立人工核对、重放和来源变化场景仍通过。修正前auto与33项结果不充当该边界的最终验证。

最终 `contacts-final-clippy.log` 的所有target/feature严格Clippy以 `-D warnings`
通过；`contacts-final-resident.log` 原欢迎24/24通过。`contacts-harness.log`
目录运行器8/8通过；最终format、Markdown与diff检查通过。测试目录运行器、正文事件fixture及doctor使用本地证据；未创建远端PR。本批未验证真实模型与生产通道；不依赖住客加好友，也未主动联系住客。

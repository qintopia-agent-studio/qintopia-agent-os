# 统一人员与欢迎 V1：本地实现与验收

> 后续状态更新（2026-09-18）：相关人员工作台和本地欢迎代码已于 2026-09-15 经
> [PR #704](https://github.com/qintopia-agent-studio/qintopia-agent-os/pull/704)
> 合并至主线（`171e227`）。下文保留当时的验收记录；“未提交”、旧分支、临时目录及暂停合并描述不代表当前开发入口或待合并状态。当前实现范围与未完成接入见[模块说明](../../runtime/sidecar/src/person_collaboration/README.md)。合并不证明真实身份、统一业务授权或生产迁移完成，历史测试结果也不是本次重新执行的结果。

日期：2026-09-09。Owner：PatrickLiveCool。状态：欢迎领域部分本地能力已实现，通用人员职责/训练授权尚未完成，A3/A4 外部接入未完成；未部署、未激活。

用户纠偏后的判定：此前实现将长期权限收进
`welcome_grants`，未统一既有二花训练授权，也未提供通用协作找人能力。下列历史测试结果只证明对应欢迎行为，不能作为统一人员基础设施完成证据。纠偏要求已写回唯一共同契约第 5.3 节；本次纠偏记录更新不代表代码重构已完成。入住欢迎人工工作台不是人员任职设置入口，当前没有需要用户填写的配置步骤。

- 唯一业务/接口协议：[共同契约](../plans/active/unified-person-welcome-v1-contract.md)
- 数据设计：[版本化设计](../../runtime/postgres/docs/data-design/2026-09-09-resident-welcome-v1.md)
- 运行入口：[workflow README](../../workflows/resident-welcome/README.md)

## 实际实现边界

| 切片 | 本地已实现                                                                                                                                    | 尚未完成                                                                                       |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| A1   | 沿用 persons.id；有命名空间的来源身份、逐入住人案例、申请关联、确认/撤销、新建 Person、范围授权；Inbox 与 WorkItem 同事务；审计和操作幂等     | 全系统 Person 合并工具和生产任职来源接入；不由本流程改写其他域历史                             |
| A2   | 原始字节 HMAC 接收、push/pull 去重、cursor CAS、逐 scope claim/fence、退避重试、扫描代次/分页恢复、最小投影/hash/营业日归一化、同版本冲突隔离 | 双程序端到端故障联调、生产常驻调度/权限/容量验收                                               |
| A3   | 人工身份候选 UI、确认/撤销/住宿绑定、受控授权和文案审批；申请回读核心、完整名单校正及自写回调过滤                                             | Feishu Workflow/QiWe 实际认证入口与 Worker 字段映射、真实名单分页/转换适配及独立操作者平台认证 |
| A4   | 条件汇合、持久制卡请求、合成 Artifact 登记、申请附件 intent/hash 回读登记、追加计划、逐目标/图文 action、执行门禁、unknown 恢复与人工核对工具 | 旧 renderer 源码迁入、字体/输入白名单、真实申请 Base 上传实现和图片预览审核、真实渠道执行适配  |

这些不是四个全部完成的生产切片。A3/A4 已完成可测试的领域核心，外部适配仍需继续开发与联调。通用 Operations
MCP 仍是预览，不冒称已创建欢迎任务；欢迎 Store 自行持久创建 WorkItem。新 capability 默认 disabled、无 allowed_callers；既有生产工作项和流程未改写。

核心代码位于 `runtime/sidecar/src/resident_welcome/`。追加迁移为
`202609090001_resident_welcome_v1.sql`、 `202609090002_resident_welcome_recovery.sql`、
`202609090003_resident_welcome_conflict_quarantine.sql`。registry、workflow 检查器、restart-target 规则和 sidecar 文档已同步。

## 合成验收覆盖

以下“通过”仅指所列本地断言，未将一个大测试名称当成 T01—T16 全部通过。

| 契约 | 已验证的本地行为                                                             | 剩余证明                                    |
| ---- | ---------------------------------------------------------------------------- | ------------------------------------------- |
| T01  | 无有效申请的案例不具备制卡资格；Person 可独立存在                            | 实际申请字段/同意语义映射                   |
| T02  | 两个 occupants 只确认选中的一个；撤销旧链接使资格失效；新建人员操作幂等      | 多人实际匹配 UI 的业务验收                  |
| T03  | 成员先于入住、入住先于名单、审批最后及名单最后均按目标汇合；预订不能正式发布 | 真实老群成员的首次完整同步                  |
| T04  | 同次住宿退群重进、重复准备不重新发送；稳定 case/phase/target/part            | 全系统人员合并、新住宿流程的完整组合        |
| T05  | source/物业/namespace 边界、错误操作者 scope 拒绝、独立人工身份门禁          | QiWe 与官方 ID 转换的账号/应用范围实测      |
| T06  | 换楼栋撤旧产物；身份/发布授权撤销、舍长离任拒绝未发；成功动作保持终态        | 实际任职来源与同意撤回组合                  |
| T07  | 基线扫描不创建伪 PMS 事件、不授予欢迎资格                                    | PMS 事务回滚、历史补录、preview 由 PMS 证明 |
| T08  | 页检查点 CAS、旧扫描代次拒绝、游标不倒退；换库存的完整向量收敛               | PMS 晚提交/发布水位和并发跨页联合验证       |
| T09  | 并发重复单 Inbox；同 event_id 异 body 冲突；整页冲突回滚                     | 双程序 ACK 丢失故障联调                     |
| T10  | 签名、时钟窗口、作用域和旧事件新签名重放                                     | 实际凭证轮换/撤销部署演练                   |
| T11  | 分页失败不推进、重启恢复待处理投影、重建禁发；冲突不被同版本回读解封         | 真实 feed 的 400/403/410 故障联调           |
| T12  | Inbox/发送 claim 过期 fencing；sending 过期成为 unknown；unknown 不自动领取  | 真实 provider 查询结果与延迟回执联调        |
| T13  | 社区成功与楼栋确定失败独立；图文独立 action/回执                             | 真实渠道部分发送失败验证                    |
| T14  | intent 幂等、hash 不符拒绝、成功上传的登记恢复、附件追加/自写回调过滤        | 真实上传/下载 hash/字段并发更新             |
| T15  | 不完整名单不执行校正；外层 inviter 不作为成员输入                            | 实际分页、成员事件、转换与认证适配          |
| T16  | 影子/live 外部执行硬关闭、基线积压不升级；重建保留禁发                       | 生产单执行者 epoch、切换与回滚演练          |

## 检查结果与诊断记录

- Rust 全特性回归：712 passed、37 ignored。忽略项依赖各自显式环境门禁，未冒称全部执行。
- 欢迎流程隔离 Postgres：2 组通过，使用显式 `QINTOPIA_WELCOME_TEST_ENABLE=1`。
- 原运行时检查：698 项默认测试和 Operations/Xiaoman
  smoke 通过；之后的新增代码用全特性回归覆盖。
- Clippy no-default-features 和 all-features、registry、workflow、Operations
  MCP、anti-drift、secrets、restart-target 检查通过。
- 工作台浏览器加载通过；实际布局宽度 1581 和 381 像素均无水平溢出，无缺少标签的输入框。HTTP 黑盒 7 项断言通过：最新 HTML、会话、Origin、重复头、范围状态及搜索。
- 本任务文档/共同契约的 Prettier 和 Markdown 检查通过；`git diff --check` 通过。
- 全仓 heavy 聚合检查受任务开始前文档格式问题影响，不能记为通过。

测试库是本任务专用容器 `agentos-welcome-v1-test`，仅映射 loopback 55439，库名
`qintopia_test`。测试数据全部合成。没有读取或连接生产数据库，也未修改 PMS 仓库或其他测试容器。

遇到并处理的工程问题：

1. 本机缺少 Rust：使用临时目录的 Rust 1.96 工具链完成本地测试，没有修改项目工具链契约。
2. 系统 Python 3.9 不兼容既有 QiWe 测试：改用本机 Python 3.11，既有测试通过。
3. 扩展已应用的草稿迁移触发 SQLx 校验和拒绝：恢复第一份迁移，将恢复结构拆为下一切片。补齐追加迁移设计引用后重建本任务合成库，最终迁移全新安装及业务测试通过；未修改生产迁移历史。
4. 全特性测试初次因沙箱禁止模拟服务 loopback 监听而失败；在允许本地测试网络的执行环境重跑后通过。
5. 一次身份集成测试出现 role_scope_forbidden；定向复验和后续完整隔离测试通过，未据此放宽权限门禁。未定位到稳定复现，保留为联调观察项，不声称已证明其根因。
6. 全仓格式/Markdown 检查包含未提交的历史交接材料、其他小满计划及研究笔记。本任务新增文档和更新的共同契约单独检查；保留其余文件，不为了全绿改写无关材料。

## PMS 差异与待联调

收尾已停止本任务本地工作台进程和专用测试容器，保留合成库状态及临时 Rust 工具链供复验。工作仍在
`codex/unified-person-v1-contract`，没有提交；原有 AGENTS.md、小满计划和研究笔记改动均保留。

D-01—D-08 的技术 DTO/hash/游标含义已收于唯一共同契约第 10/11 节。

PMS 原任务最终回传接口与共同契约兼容，无新增协议差异。扫描包装、最小投影、墓碑和共享 fixture 的 hash 一致；PMS 侧 2 项 hash 测试通过。本任务亦只读核对了
`scanPmsOrders` 的包装；未修改 PMS 代码。

共享 `order-projection.json` 的固定 hash 为：

```text
b46f00b6121e9f6bda068dcbef5b7219c4f35dee0b07d82b21d46c6a1cc79399
```

这不是双程序已联调证明。PMS 的事务捕获、Outbox、已提交发布水位、真实 HTTP
DTO 和投递由原任务负责。待用其最终回传版本完成 push/pull 原始字节、ACK 丢失、410 重建、墓碑和营业日边界的双端联调。单物业 READ 沿用既有权限；客户端白名单不等于 PMS 凭证具备接口级权限隔离。

## 外部依赖与下一切片

旧 `generate_card_v10.py`
不在本地可见源码内。本次只读 SSH 返回 publickey 认证失败，未运行旧脚本或其 dry-run。

此前评估记录有一次成功静态检查，与本次访问失败属于不同时点的运行证据。旧 renderer、字体与模板仍需可读取的版本化源码；不能把合成 PNG 登记称为迁入完成。

申请 Base 的「欢迎卡片」是唯一拟写字段，与阿靓设计产出 Base 分离。

现有 Huabaosi 存储适配器绑定设计产出 schema，不能直接用于申请表；当前只实现 intent、回读登记与纯追加计划。真实 Worker 还需受控申请字段映射、固定资源配置、已验证上传回读和并发追加策略。

QiWe 实际名单分页/转换、平台认证与独立人工操作者仍须接入，不能用本地进程选定的合成人工身份冒充生产认证。

部署、生产迁移、真实 Base/Workflow/权限配置、Hermes
profile、真实制卡/上传/消息发送均未执行。旧生产流程继续运行。生产批准前须完成剩余适配、业务验收、单执行者切换和 unknown 回执演练。回滚先停新副作用并核对在途/unknown，保留身份、Inbox、case、action 和审计；不删除数据重放积压。

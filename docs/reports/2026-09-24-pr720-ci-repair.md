# PR #720 跨平台 CI 修复

## 失败证据与处理范围

原 head `ee00407` 的 CI run `35849676206` 和业务测试 run `35849676200`
失败。本轮在原 PR 分支修复，不合并远端、不部署；保留主工作区五份规格和前台工作树。

- Light check：新增岸岸草稿包及 Person
  Foundation 工具未登记重启归属。曾尝试修改重启路由，此修改越过基础设施边界，现已撤回；原未匹配问题仍未解决。
- PostgreSQL：欢迎 HTTP 测试缺少显式 Python 渲染解释器；须在既有测试入口解决解释器依赖，不修改 Evans 制定的 CI 流程。
- 任期编辑：Linux 的纳秒时间经 PostgreSQL 微秒存储后不完全相等，误触发替换连接。按数据库微秒精度比较，补确定性的纳秒输入回归。
- 卡片修订：缺中文字库时不同中文可能渲染为相同缺字图形。将原始渲染输入摘要绑定到 PNG，防止视觉碰撞掩盖内容变化。

当前仅完成恢复及必要验证，按负责人要求暂停 PR 排障和合并。聚合 check 的失败来自上游；Release
Please validation 的跳过不是独立错误。

## 制卡归属纠正

负责人确认：欢迎图片已由四老师 Runtime 内部能力成功实现，后续迁给阿靓。本轮未定位已有源码不等于该能力不存在。具体渲染归智能体能力包；平台只协调受控任务、输入许可、产物版本和结果。本轮仅修合成测试替身，不替代或启用正式渲染实现。

## Reviewer 建议处置

旧 head 的 Reviewer Guide 建议将 `parts`
解释为“仅发送这些部分”。现有契约实现将其作为直接发送的授权范围，未覆盖部分仍需内容审核；原回归
`foundation_welcome_timing_permission_change_and_scope_fail_closed`
已断言文字直发、图片等待审核。

本轮保留该兼容语义，不把范围外部分改成自动丢弃。当前 UI 和可信设置工具明确配置卡片与文案两部分；未提供“仅发文字”的开关。给类型补说明，避免误解为发送选择列表。没有以 PR-Agent 通过代替人工或工程验收。

## 验证结果

本地渲染 3 项通过，覆盖像素完全相同但输入修改时产物摘要必须改变。此前新增路由上的专项通过和完整 PR 路径无未匹配结果已失效，不作为当前验证证据。固定纳秒任期、内容更正撤旧和欢迎 HTTP 两分支共 4 项 PostgreSQL 回归通过。

首次复验遇到隔夜测试数据库进程已退出，恢复原隔离库后重跑通过；没有重置数据或放宽测试门禁。跨越恢复操作的完整检查不作为最终提交通过证据；远端结果不得沿用旧 head。

## CI 与基础设施约束纠正

负责人再次确认：不得修改 Evans 制定的 CI 流程和本项目基础设施，不得通过跳过检查、降低断言或调整基础设施消除失败。

`39bf1c2` 越界修改的六个文件均恢复到 `ee00407` 内容：

- `.github/workflows/ci.yml`
- `.github/workflows/business-tests.yml`
- `tools/ci/check-ci-contracts.mjs`
- `deploy/restart-target-rules.yaml`
- `tools/deploy/test-resolve-restart-targets.mjs`
- `tools/deploy/README.md`

前三项已在 `2d5a526`
恢复，本次恢复后三项。保留时间精度及合成渲染输入摘要的业务修复。解释器缺失和路径匹配仍未解决，不宣称 CI 通过；先恢复，再单独处理 PR 未合并问题。

恢复基准是本轮修复前的 `ee00407`，并非
`origin/master`。前序交付中仍有 CI、部署和工具文件差异，尚需逐项审计归属；本报告不代表整个 PR 已无基础设施差异。未部署或修改服务器，五份未提交规格及前台草稿均保留。

## 恢复合并前的纠正

负责人已允许必要的 CI 调整，但要求说明依据、最小范围与复用价值，不增加业务特例或将本地测试引入生产部署。当前 PR 无文本冲突，Light 路由与 PostgreSQL 欢迎 HTTP 测试失败。

本次先恢复 CI
workflow、CI 契约和本地检查入口；业务测试自行启用本地 HTTP，自动化欢迎链使用固定 PNG 测试产物验证版本、审批与恢复，真实字体排版留在可选本地视觉验证。

共享测试框架不注入业务变量或依赖 Pillow。保留已获认可的路径解码和临时目录隔离修复。迁移及草稿登记须处理其原始不合规原因，不新增门禁例外。未修改线上 Profile、数据库或发送渠道。

负责人随后明确批准 006 方案：仅补设计注释，旧库和旧提交保留，新建本机独立数据库验证，不修改旧库 checksum。原 SQL 字节已额外保存在本地忽略证据目录；Git
`3030d67` 也可恢复。该决定替代上文冻结特例做法，不可套用于已发布生产迁移。

负责人明确批准唯一新增部署配置：在 `hermes-erhua` 现有 paths 下登记
`skills/person-foundation/**`，依据二花真实加载入口，沿用原流程。岸岸本地脚本与模板迁入
`fixtures/agents/anan/`，从生产 Agent 注册清单移除测试替身，保留能力文档；负责人已建立的真实 Hermes 岸岸不受本次修改影响。旧“完全恢复重启规则”的结论仅描述此前撤回阶段，当前以此明确批准的一行归属为准。

最新 Reviewer Guide 的 `parts`
建议沿用上文处置。审批幂等分支遗漏准备恢复的问题已修复：同一审批重试先重新核验授权，再恢复幂等准备，不重放发送；专项模拟审批已落库而岸岸暂不可用，验证恢复后只有一条动作且零发送。

同样属于脚本替身的阿靓、二花欢迎处理器也迁入
`fixtures/agents/`，避免仅测试代码变更造成生产 Agent 重启归属。真实二花工具注册不动，正式制卡能力不迁移、不替换。

## 最终本地验证与长期约定

`pnpm check:pr:auto` 完整退出码为 0，包含 Light、heavy Rust 与 PostgreSQL tiers；最后的
`operations-control-plane-apply-smoke.sh` 通过。默认 Rust 为 851 passed / 2
ignored，all-features 为 866 passed / 123 ignored；独立 PostgreSQL 人员协作为 71 passed
/ 0 ignored，欢迎为 24 passed / 0
ignored，包含新增审批重试恢复回归。ignored 不作为通过证据，数据库业务由独立集成套件执行。

Python 可信工具 7 项、岸岸夹具 2 项、欢迎协议与固定产物 10 项及可选视觉 3 项通过。原 CI 契约、反漂移、部署工具测试、重启归属预览、格式与 Markdown、协作检查、PR
doctor 和实际 PR 正文校验均通过。远端结果仍须核对最终推送的 head，不沿用旧提交结果。

负责人要求的九项 CI 评审原则已写入 `AGENTS.md` 入口与
`docs/engineering/programming-agent-guardrails.md#ci-change-approval-rule`：先评审、汇报建议，明确同意后修改；开发或合并授权不替代 CI 修改批准。本轮只实施已明确同意的恢复与最小部署归属调整。

主工作区的未提交规格、真实岸岸接入文档及前台草稿保持原样；006 旧库及 checksum 保留。本轮未执行部署、线上 Profile 修改或真实发送，真实 Runtime 联调和前台续作仍为后续事项。

## 推送后审阅补充

`7a99794` 的 Reviewer Guide 新指出执行器不可用分支先于动作状态查询，导致既有 `succeeded`
或 `unknown` 被误报
`waiting`。本次将持久状态回读提前，保留未知结果禁止重发的规则，扩展既有端到端恢复回归，覆盖准备态等待、成功态回放和未知态回放。此修改仅涉及欢迎业务，不修改 CI。

扩展后的 `foundation_welcome_direct_review_real_agents_and_partial_unknown_recovery`
在独立 PostgreSQL 上通过（1 passed / 0 ignored），且发送副作用总数不增加。

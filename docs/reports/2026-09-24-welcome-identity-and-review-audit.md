# 欢迎身份关联与运营确认审计

日期：2026-09-24。状态：真实只读审计及确认接线方案；未完成生产接线、自动身份落库或业务验收。

## 决定与来源

来源为任务 `01a0c959-5c49-72e3-8de1-5711e06fd964` 本日用户六条批注，所选响应
`msg_01e52a302fbc3ce8016ab4f64465f087d0a6cb5d0f986f0e19`。以下区分明确决定和待评审建议。

1. 用户：“OK 那就这么定了，我已经把岸岸这个智能体拉到我们的工作群了‘全村人运营团队’”。岸岸负责发起卡片、文案的客房欢迎确认。
2. 用户：“可以由岸岸直接来做这件事情，就不用再绕到四老师去了”“把沟通、确认这些工作都放到工作群里面来做”。岸岸直接呈现、跟进修改和反馈，不要求四老师逐次转发。阿靓制卡、二花依本栋规则办理后续欢迎的职责不因这次决定改变。
3. 用户：“谁有权利，需要在UI里能设置，确认的目标群是哪个需要能设置”。群与 UI 操作同一事项，同步当前版本和结果；客服、社区主管在有效权限内补位，群成员身份本身不授予确认权。
4. 用户：“以后都说‘模拟’”“可以看懂微信昵称+姓名这样的组合”“昵称有时候会改”。面向用户使用姓名、微信/企微昵称及必要的岗位、范围信息；未知姓名如实显示，不猜测补齐；不要求用户识别内部 Person
   ID。底层仍用稳定人员标识，账号昵称只是可变显示属性。
5. 首轮用户要求简化共享账号确认；当时尚未决定归责模型。同日后续批注已明确批准“秦托邦小客服”企微工作账号直接作为小客服主体，不追溯轮班自然人，以下补充决定取代此前待定状态。
6. 用户：“需要你通过wecom，qiwe的各种接口能力完成关联，人确认是不可行的”“根据现有的数据就能审计模拟是否可以建立可靠关联”。本次采用真实数据只读核验，不把逐人手工绑定作为默认解决办法，不凭昵称、姓名、头像、同群或付款关系自动认定入住人。

既有范围：[人员欢迎共同契约](../plans/active/unified-person-welcome-v1-contract.md)、
[身份基础评估](../plans/active/unified-person-identity-foundation-assessment.md)。本次最新决定覆盖旧文档中与上述责任分工冲突的建议，但不表示接线已经实现。

## 审计方式和边界

使用已经获准的服务器接线读取现有 Agent
OS、PMS 数据库与 QiWe/官方企微接口。数据库连接强制只读事务与查询超时；凭据只在服务器进程内使用，未输出、复制或保存凭据。仅保留计数、字段名、错误码与结论，不保留成员资料、手机号、账号或原始消息。

首次通过现有用户权限读取飞书表结构和记录数量；同日补充审计仅投影手机号，完整读取 440 行用于临时比对，原始投影和临时匹配摘要均在统计后清除，仅保留脱敏计数。官方企微抽样使用 PMS 已有企业应用权限，不使用岸岸 Bot
Secret 冒充客户联系应用凭据。

当前生产 release 为 `16e8d56b98001579c6288ba13199b80d6d3dfc74`。PR
720 已合并不是该生产版本已安装的证据。本轮没有改生产文件、权限、数据库、身份绑定、网关或 CI，也没有发送群消息。

脱敏临时证据保存在原工作区忽略目录 `.local-workspace/identity-link-audit-20260924/`：
`read_identity.py`、`identity-counts.json`、`read_wecom.py`、`wecom-counts.json`。未将真实业务快照加入 Git。

## 首轮已观察到的结果

| 核验对象           | 结果                                                                                                     | 能证明什么 / 不能证明什么                                                         |
| ------------------ | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Agent OS 人员      | 184 条 active Person；群成员来源账号 285 行、278 行有 person_id；联系人来源 327 行、131 行有 person_id   | 已有账号归档基础；行数不是独立人员数，旧关联不能直接当作 PMS 实际入住人确认       |
| 新来源关联与欢迎表 | `source_identity_links` 为空；申请、欢迎案例、成员、来源、来源版本、协作租户表均为 0                     | 新欢迎链路尚无生产端到端数据证据                                                  |
| QiWe 读群          | 配置社区群 499 人，一栋 35 人、二栋 16 人；可读 userId、name、roomRemarkName、joinTime、inviterId 等字段 | 能识别平台群成员账号；邀请者和事件操作者不能当作新人                              |
| QiWe 账号转换      | 三群分别抽样 20、20、16 个账号，请求均返回 40005；单账号请求同样失败                                     | 当前网关未支持转换，不能将失败解释成真实匹配率为 0                                |
| 官方企微客户详情   | 3 个已有付款客户账号抽样全部成功，返回 external_userid 与 unionid；返回账号与请求一致                    | 官方稳定账号标识可读；样本仅代表已有付款客户，不代表全部居民或全部账号            |
| 官方企微补充字段   | 3 个样本均无备注手机号；1 个有 follow_user.state                                                         | state 尚未证实是申请引用；unionid 需核对开放平台作用域，不能无条件跨企业/应用拼接 |
| PMS 会员和申请引用 | 未删除会员 12，均有 phone、wechat 文本；`member_external_references` 为 0                                | 有文本资料，没有已存储的申请来源关联；自由文本微信号不是官方 external_userid      |
| PMS 实际入住人     | 103 人次，9 有手机号、0 有证件号；100 订单中 11 有 member_id；入住人行无 member_id                       | 订单关联会员不代表同单所有实际入住人，现有资料不足以直接证明逐人账号对应          |
| PMS 支付关联       | 90 条匹配对应的付款记录均有 external_user_id；83 关联订单，2 条关联到订单 member_id，涉及 61 个付款账号  | 可追踪付款账号和订单；付款人不能自动视为入住人                                    |
| 飞书申请表         | 440 条记录、20 个字段；有姓名、花名、手机、照片、展示同意等，无微信账号或企微 external_userid 专用字段   | 尚无直接渠道账号键；未读取全部申请内容，未计算姓名/手机号候选匹配率               |

QiWe 实际错误为：“网关未配置老平台转发（legacy_api），该接口暂不支持”。
[供应商转换文档](https://doc.qiweapi.com/api-344613881)描述 `/contact/openid`
返回官方external_userid，但文档存在不代表当前接入已启用。本轮未开启转发、扩权或更换网关。

现有组织 UI 源码支持姓名/昵称搜索和选人，候选来源为人员表及已确认来源关联；本地响应仍标示模拟模式、未连接 Runtime。因此不能把本地可选模拟人员说成已接上真实完整人员档案。

## 自动关联结论及最小后续路线

本次证明了稳定渠道账号有可用接口基础，但没有证明“进群账号 → 申请 →
PMS 逐个实际入住人”的可靠完整关联。不能报一个未经计算的成功率，也不能用候选相似度代替可靠身份。

建议后续依次验证，具体新接口、来源写入和生产配置按各自授权实施：

1. 查明当前 QiWe 转换入口的受支持接法；优先现有能力，不为一次测试改基础设施。核对企业/应用作用域，使用真实返回的账号映射复测一栋、二栋。
2. 核查已有 `follow_user.state`
   的来源和语义，检查原申请入口是否曾保留可信渠道引用。现有样本仅证明 state 存在，不证明它包含申请关系；没有依据时不猜测。
3. 若历史证据确实不足，在正常申请或接待数字入口自动携带经过验证的渠道账号，并关联申请及 PMS 逐人入住引用。可评估同一开放平台下的微信登录与 unionid 对接；属于待验证方案，不承诺现有飞书表单天然提供此能力。仅发送专属链接不能证明填表人就是目标账号，仍须可信登录/回调上下文。
4. PMS 输出住宿事实与来源引用，Agent
   OS 维护共同身份、任职、任务和确认记录；不要求工作人员再建一份人员台账。同行人、代付、换号、多人共用账号及歧义分别保留状态，不能为提高覆盖率误合并。
5. 入群事件触发欢迎处理，尚未建立可靠关联时停在“暂未对应入住资料”，不得展示别人的卡片或自动发送。实际入住、内容许可等既有条件仍须满足，除非用户明确改变该业务规则。

## 同日补充：本栋欢迎词、工作账号和手机号关联

来源为同任务后续四条用户批注，所选响应为
`msg_03c29fbdaa829359016ab5017d818087d0bbe4f198bbcc7b56` 和
`msg_03c29fbdaa829359016ab501f1873087d0bb5dd9b9a4286de6`。

### 已明确决定

同日追加批注纠正名称为“秦托邦小客服”，并明确“这个企业微信号应该是可以设置的”。来源为本任务对响应
`msg_03c29fbdaa829359016ab503c21b7887d0b7eec7421ade4ff8`
的用户纠错批注。本文相关名称按纠正后的写法统一；旧引用中的错别字不作为业务标识。

小客服岗位对应的企微工作账号是可配置项：由具备相应管理授权者查找、选择和更换已验证账号，绑定稳定渠道标识及企业作用域，不把名称或账号常量写死。更换后旧账号的该项岗位确认授权失效，待办按当前有效授权核验；历史记录保留当时账号主体与展示名称，不改写成新账号。

- 用户：“照片和欢迎卡片是统一的”“欢迎的措辞和文案方面，还是要以本栋的舍长为优先级”。统一照片、卡片和介绍资料由岸岸发起运营确认；本栋欢迎词优先使用舍长有效安排。建议版本分别管理：只改问候口吻不重复要求运营人员确认同一份资料；若改变人物/住宿事实或新增披露，重验受影响内容与许可。本条后半为实施建议，不能把舍长措辞优先解释为取消来源真实性和内容许可。最终发送须绑定统一卡片与本栋欢迎词的实际组合版本，并满足本栋当时有效的审核规则；只改措辞也不能绕过舍长要求。
- 用户：“直接把‘秦托邦小客服’这个账号作为小客服使用的账号就可以了……不用追溯到具体自然人”。本轮欢迎确认可直接以该企微工作账号归责，显示“秦托邦小客服已确认”；不要求轮班员工另行登录证明自然人身份。底层绑定可信渠道账号标识及企业作用域，不能按相同昵称授予权限，也不伪造自然人档案。沿用明确的岗位/操作/范围授权及撤权机制；该决定不自动扩大到收款等其他权限。

### QiWe 文档复核和负责人可协助的事项

用户要求：“你读 <https://doc.qiweapi.com/>
这里的 qiwe 的文档，如果需要我手动配置告诉我做什么，我来做”。

已核对[正向转换](https://doc.qiweapi.com/api-344613881)、
[快速开始](https://doc.qiweapi.com/doc-9079966)、
[常见问题](https://doc.qiweapi.com/doc-8483505)及
[联系人详情](https://doc.qiweapi.com/api-344613868)。

正向转换页当前也标为“开发中”；先前只提醒反向转换未就绪不完整，现予更正。

请求参数为 guid、userIdList、可选 corpAgentId，返回说明为官方 external_userid。

只读核实了当前服务器所用 HTTPS 主机和路径与文档的 `new.qiweapi.com/qiwe/api/qw/doApi`
相同。不能据此认为需要负责人重新生成 Token、重新扫码或更改 Webhook。已读接入指南与 FAQ 未提供 legacy_api 的客户侧配置步骤；40005 不能靠填写一个猜测的企业应用 ID 解决。

负责人可将以下不含凭据的询问发给 QiWe 支持方；本任务未代发：

> 我们使用新版 QiWe 网关，群详情及联系人详情可正常读取。调用 `/contact/openid`
> 返回 40005，提示“网关未配置老平台转发（legacy_api），该接口暂不支持”，文档页面也标为开发中。
>
> 请确认当前租户是否支持此接口；legacy_api 由平台配置还是由客户配置；如客户可配置，请给出准确控制台位置、字段说明及是否影响现有设备/Webhook。
>
> 请同时说明 corpAgentId 的获取方式与适用企业/应用范围。若暂不支持，是否有受支持的接口直接返回官方 external_userid 或 unionid？

在支持方给出具体接法之前，尚无已验证的手动配置步骤可以交给负责人执行。配置或生产重测按具体影响另行处理，不默认切换网关或重建节点。

### 飞书与 QiWe 手机号实际比对

通过 lark-base 读取当前申请表单配置：“姓名”“手机号码”已经必填，手机号仍是文本字段，必填不等于验证码验证。分享读取返回 anyone、require_login=false；不把飞书登录标识当作微信身份。

查询范围为申请表全表，省略 View 和筛选，仅投影手机号；440 行且 has_more=false。

Python 仅为候选比对去除空白、括号、短横线和 +86/0086 前缀，接受严格 11 位大陆手机号，不纠正错字、不改源值。

431 行符合该格式，403 个不同手机号，24 个手机号对应多条申请；重复可能来自再次入住或重复提交，不能直接视为多人共号。

QiWe 读取一栋、二栋全部群成员及对应联系人详情，以联系人响应的 userId 对应请求，不依赖名字。手机号用单次随机密钥 HMAC 后做相等比对，原始申请投影放在受限临时目录并自动清除，手机号、账号明细和临时 HMAC 不作为证据保留。

| 范围 | 群账号数 / 成功读取联系人 | 有可用大陆手机号的账号 | 对应一条申请 | 对应多条申请 | 有手机号但无申请匹配 |
| ---- | ------------------------- | ---------------------- | ------------ | ------------ | -------------------- |
| 一栋 | 35 / 35                   | 4                      | 2            | 0            | 2                    |
| 二栋 | 16 / 16                   | 5                      | 2            | 0            | 3                    |

按两个群分别统计，未证明两群成员互斥；群账号包含工作人员等非住客，不能据此报告住客识别成功率。没有可用号码也可能是格式或国家区号不在本轮规则内。数字仅为候选手机号相等，不代表完成申请到实际入住人的可靠身份绑定。

首次二栋 5 个账号抽样恰好都有完整号码；扩大至全体后只有 5/16 有符合本轮规则的号码，不能用该小样本宣称全覆盖。脱敏证据为忽略目录中的 contact-field-counts.json 和 phone-match-counts.json；辅助脚本无硬编码业务凭据，未写回业务数据。

### 保留飞书申请表的建议（尚未实施）

用户提出：“还是不想大动”“比如说把手机号设为必填，或者通过身份证号……也都有填错的可能”，并要求比较“由智能体发起主动确认”与“找人对接确认”的代价。以下是建议，不是已批准的新验证服务或外发流程。

- 首选保留现有申请表。改到企微表单若只得到微信名称，同样不能消除重名和改名问题；只有能够取得稳定账号及可信提交上下文，才有身份链上的实质收益。
- 已有可靠来源直接复用；手机号相等只用于缩小候选范围。不要仅因唯一匹配就永久认人，更不能按支付账号推断入住人。
- 智能体优先在可信私聊中请申请人完成一次必要的自助核验，并确认是本人入住还是代填/同行。手机号路径可采用一次性短信验证码绑定当前聊天会话和具体申请；这证明对号码的控制，不是法定身份核验，实际住宿关系仍以 PMS 逐人事实为准。
- 当前只确认手机号字段必填，尚未发现已有短信验证服务。新增短信会产生开发、供应商及发送费用，需单独决定，不把本建议写成已具备能力。可另评估申请回执中的一次性确认码，绑定具体申请与私聊会话；必须保证回执仅交给可信提交者、码随机且有期限/单次使用，普通公开编号、可转发链接或一个“是我”回复不够。飞书是否支持所需回执接线尚未验证。
- 手机填错且不能完成核验、多人/代填/同行歧义、换号或事实冲突时，才转交秦托邦小客服。在同一事项中收集更正和处理结论，不要求工作人员重复录入所有人的台账；关联完成后复用，下次入群不重新问一遍。
- 不建议为了绑定微信额外收集身份证号。格式/校验位只能发现部分输入错误，不能证明账号归属；已有 PMS 合规证件核验可作为住宿事实依据，不复制证件到群或欢迎卡片。

从人力成本看，正常情况自助核验、异常情况客服接手优于全部人工匹配；具体短信或回执接法仍须用真实支持能力确定。一次性居民核验与此前反对的“工作人员逐人手工绑定”不同，尚未获准自动向居民发送本轮审计消息。

### 工作账号主体的现有实现差异（只读代码评估）

核对基线 b42db0d 的结果：现有 actor、授权和欢迎审计尚不能将工作账号作为独立主体完成确认。不能只移除一个 shared 拒绝条件就宣称支持，更不能给工作账号伪造自然人 Person。

| 位置                                                        | 当前约束                                                                                                                 |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `person_collaboration/store.rs` 的 Actor / verify           | person 为必填 UUID；验证 JOIN 人员，并排除 shared gateway                                                                |
| `person_collaboration/store/identity.rs` 的 gateway_actor   | 必须解析 active Person；shared 账号直接报 shared_account_person_unknown                                                  |
| `person_collaboration/model.rs` 的 Grant                    | 授权主体为 person UUID，尚无独立工作账号主体类型                                                                         |
| `resident_welcome/store.rs` 的 actor_from_verified_identity | 要求 individually_verified 并解析具体 Person                                                                             |
| `resident_welcome/foundation.rs` 的 foundation_approve_as   | 入参为 person UUID，写 approved_by，审计固定 actor_type=human                                                            |
| 既有数据库约束                                              | confirmed 来源关联必须有 person_id；collaboration_commands.actor_person_id 与 welcome_approvals.approved_by 均为人员外键 |

最小增量提案供总指挥协调，尚未实施：

1. 共同身份层显式区分 `person` 与 `work_account`
   主体。工作账号保持可信渠道/企业命名空间、有效状态及版本，不占用 Person。旧个人解析接口保持个人语义，增加面向已授权业务的主体解析，避免影响居民记忆/在住代理等依赖真实人员的行为。
2. 授权检查接受可信主体引用，工作账号只通过明确授予的欢迎运营确认动作与范围；不把旧个人授权自动复制给工作账号。主体必须由 host 会话解析，模型不能自报类型或账号。
3. 欢迎运营确认记录及命令幂等记录明确保存主体类型与引用，审计显示“工作账号确认”，自然人未知就不填自然人。使用新增迁移及数据设计，不改已应用迁移；具体表结构和迁移编号需与活跃任务协调。不能为了省字段把工作账号 UUID 塞进人员外键。
4. 本体线负责 `person_collaboration/model.rs`、`store.rs`、`store/identity.rs`
   及欢迎接收/审计的最小增量和必要迁移；宿主/工具只传可信上下文。岸岸继续提供来源、住宿、事项引用；其 PMS 确认保持既有检查，不自行删去个人验证条件来绕过共同接口缺口。公共 server/mod 改动先报总指挥。

上述增量仅定义需求和兼容方向，不代表已批准把所有业务改为工作账号确认。需覆盖本人账号兼容、工作账号伪造拒绝、精确权限、撤权、审计显示及本栋实际组合版本验证。

## 欢迎公共入口最小接线提案

本节供两条开发线确认接口语义，不是已实现 API 或新的 PMS 执行协议。

总指挥任务 `01a0c6ee-1de7-7302-8be1-3f7966f51bea`
于本日转达用户新决定：每笔登记收款必须有人确认，长期授权不能替代；原完整明确指令可作为当笔确认，不重复询问。PMS 主动向 Agent
OS 发送收款事件；事件发送和 head 接口由 Green
PMS 项目承担，岸岸负责接收和处理。该决定不改变欢迎内容确认与 PMS 业务确认分离，相关跨仓接口仍由对应任务负责。

| 边界       | 最小约束                                                                                                                                                    |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 输入       | 可信来源事件/任务引用与版本、幂等键、物业/楼栋范围、已核验 Person/逐人住宿引用；卡片产物及文案版本、展示许可依据。缺失身份可保存待关联事项，不进入确认/发布 |
| 服务端解析 | 从可信会话、人员或已授权工作账号、岗位任期与授权解析岸岸身份、当前确认者和目标群；不得接受模型自报审批者、权限或群名作为执行依据                            |
| 输出       | 同一个持久事项引用、当前状态、等待原因、内容版本及配置版本，供群与 UI 展示；内部 ID 不作为用户辨认人的方式                                                  |
| 确认效果   | 有权客服或主管确认“该人、住宿、介绍、卡片对应正确”；只推进欢迎内容/资格阶段，不代替 PMS 订房、收款、入住确认，不等于发布成功                                |
| 版本与并发 | 统一卡片/介绍确认与本栋欢迎词分别绑定各自版本；受影响内容的改版使旧确认不能用于新版。群与 UI 同时办理只产生一次有效状态迁移；迟到的旧消息不能批准新版       |
| 权限与恢复 | 每次动作重新检查任职有效期、范围、撤权和当前配置；无人有权则保留待处理。发送结果未知保留 UNKNOWN，不自动重发；恢复沿用原事项和幂等键                        |

UI 提供业务可读的“谁可以确认”“发到哪个工作群”，支持查找人员、选择岗位任职范围及生效期限。群显示名称与可靠渠道标识绑定，不要求用户填写裸 ID。配置变更应明确待办受影响范围；不能把所有历史任务静默转发到新群。

群中以岸岸发出的具体事项卡片/引用作为确认对象；普通无指向的“同意”不能误批其他人或其他版本。内容修改和停止也要指向同一事项。工作群呈现的是获准用于该确认的卡片、文案与必要信息，不包含身份证号等无关住宿资料。

## 交接与验证边界

已阅读根规则、README、路线图、变更路由、工程护栏、架构/产品边界、双线协调、QiWe 包规则、服务器规则及 Green
PMS 仓库规则。此次修改仅为本报告及报告索引；未修改公共 Rust 文件、迁移、Agent
Profile 或 CI。

岸岸线继续 PMS 查询/办理与业务确认，不另建身份库或欢迎确认 UI；基础线负责共同身份、友好选人与欢迎群/UI接线。双方公共文件增量由总指挥协调，预留迁移
`202609240001_business_operation_execution.sql` 不被本次占用。

本次实际核验为真实只读数据库、QiWe 与官方企微 API 调用，以及文档格式/Markdown/相对链接检查。独立文档变更的
`pnpm check:pr:auto` 选择 quick
tier 并通过，Markdown 检查覆盖 442 个文件、零错误。没有把报告和工程检查计作真实业务闭环通过，没有重新声明 PR
720 的历史测试为本次新业务通过。

待完成：QiWe 转换接法、申请与实际入住人证据链、真实人员候选、运营群可靠标识和岸岸渠道呈现、有效确认与改版/补位/撤权回归、整合版本人工验收。工作账号确认已经获准，可靠账号绑定及相应授权接口仍待实现。完成本地业务接线及验收后再按发布流程部署试点；本报告不授权部署。

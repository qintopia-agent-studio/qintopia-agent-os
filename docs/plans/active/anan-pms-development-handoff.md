# 岸岸客房协作：新开发对话交接

日期：2026-09-24。状态：已从 PR `#720`
合并后的主线创建独立开发任务，确认已建分支并继承规格。PMS 工具和生产接线尚未完成；实际开发结果由新任务持续更新。

完整需求、业务流程、接口依据、分期与 T01—T14 验收以
[开发计划](anan-pms-event-integration.md)为准。本文件提供执行入口，不另立业务规格。将下面的提示词完整复制给新开发对话；该对话应先继承最新工作区文档，再实施本地代码。

总指挥补充的[架构与并行开发评审](../../reports/2026-09-24-anan-architecture-parallel-review.md)列出能力包组织、共享文件责任、旧基线与迁移编号冲突。开始前一并读取；文件分工须在实际接手时落实，评审本身不代表已启动另一路开发。

## 最新调度基线与分工

- 两条活跃任务共同遵循[双线协调记录](agent-os-dual-track-coordination.md)。基础线负责身份审计与欢迎确认群/UI 接线；

  岸岸负责 PMS 对话办理、事件与专属共享增量。公共文件按接口语义整合，阶段交付和冲突直接回报总指挥。

- 任务创建已受理，应用返回
  `client-new-thread:e0e1916d-705b-434a-bac8-4803c4d391f7`。接手工作区为
  `/Users/feather/.codex/worktrees/7243/qintopia-agent-os`，已创建
  `codex/anan-pms-collaboration`；2026-09-24 09:35
  UTC 核对 13 份继承文档哈希全部一致。随后收到实际任务 ID
  `01a0d2c3-4d3e-7f72-9206-fce386254bef`，已通过任务工具确认正在执行；规格继承提交为
  `adff875d829c70b1fa938318505bb3f6b81824bc`。
- PR #720 已于 2026-09-24 09:14:19 UTC 合并；本次启动固定基线为
  `b42db0d8b8388c65dcf62c1f1fd41a7325298c5e`，核对时也为 `origin/master`。
- 用户明确要求另起任务并隔离 worktree、分支、测试数据库、端口和 socket。由应用创建独立工作区，使用
  `codex/` 分支；保留待验收材料，完成验收及合并、保存证据后再清理。
- 当前根工作区的 13 份未提交文档已完整保存在忽略目录
  `.local-workspace/anan-dispatch-20260924-b42db0d/`；新任务继承核对过合并差异的规格补丁，不回写或重置根工作区。
- 合并版将岸岸欢迎测试替身移至 `fixtures/agents/anan/`；`agents/anan/`
  目前保留职责文档。正式 PMS 工具按能力包契约实现，不能把测试替身重新登记成生产 Profile。
- 岸岸任务负责
  `skills/pms-operations/`、岸岸集成、客户端和事件消费者，以及本批所需的官方 WeCom 可信上下文、持久确认和执行记录的最小共享增量。共享修改单独列明接口、兼容性和迁移，

  保持现有欢迎回归；不重写共同授权、不扩展授权大类。

- 总指挥负责社区线与岸岸线的接口和合并顺序；前台旧工作区及其碰撞迁移不由岸岸修改。共享契约若有业务语义冲突，回报总指挥并继续无关工作，常规实现和测试不重复请求开工许可。
- `registry`
  和既有测试清单遵守能力登记契约。用户已专项批准“身份登记与受管部署分离”的 A 最小方案，由岸岸任务记录并实施：1 个 manifest
  schema、2 个检查器、1 个回归文件、1 个既有检查入口追加、2 处契约文档及正常 Agent 登记。缺省 managed 行为不变，unmanaged 不得进入生产清单；不包含 resolver、restart
  rules、安装 payload 或生产接管。范围外 CI 调整仍须遵守已合并的专项审批规则。
- 先完成可独立验收的 PMS 对话办理，再继续无外部阻碍的收款、申请和欢迎接线。授权仅覆盖本地实施、测试、提交与按项目规则准备 PR；生产及外发边界不变。
- 飞书原申请入口已有证据：见[历史现场核查](unified-person-identity-foundation-assessment.md#61-本轮只读现场结果)及[四老师现有 Bridge](../../../workflows/silaoshi-daily-ops/README.md)。从
  `silaoshi-base-notify`、官方 script
  transform 和持久 runner 复用本地适配；不因参考目录为空就另建扫描器。仅新增申请触发已有证据，修订/撤回及当前生产绑定尚待核实；旧制卡直发不能与岸岸→阿靓→二花并行执行。
- 支付首次基线发现真实 API 缺口：按时间判断会漏掉延迟提交事件。见[最小事件头接口提案](anan-payment-feed-baseline-proposal.md)。

  该提案列明 5 个 PMS 文件、只读语义和并发验证；首次序号起点已获用户同意，接口与主动投递归 Green
  PMS 项目，见[该项目交接](green-pms-payment-events-handoff.md)。本线保留 feed 补拉/恢复，不跨仓改源码，不把轮询叫主动推送。

每笔 RECORD_COLLECTION 必须绑定有权人对具体订单、流水和金额的明确确认；完整原交办可以复用，不因到账事件、岗位或长期授权而省略当笔人工确认。

## 可直接复制的提示词

```text
开始实施岸岸（anan）的完整客房协作能力，本地开发已授权。

项目：/Users/feather/Documents/Codex project/qintopia-agent-os
先读取并执行以下完整交接，而不是只按本提示词摘要开发：
docs/plans/active/anan-pms-event-integration.md
docs/plans/active/anan-pms-development-handoff.md
docs/reports/2026-09-24-anan-architecture-parallel-review.md

目标：岸岸配合小客服和刘珊，通过企微对话操作 Green PMS。
先完成不依赖事件的查询、建议/预览、有效确认、PMS 执行、回执与恢复闭环；
再接 PMS 收款事件和飞书入住申请事件，最后复用既有欢迎及工作跟进。
优先向小客服确认，无法办理再交刘珊。普通订单不逐笔追加主管或四老师审批。

接手先读项目 AGENTS.md、README、路线图、变更路由、工程护栏，以及受影响包契约。
核对 Git 分支、提交、工作树、现有 PR/合并状态和两仓接口版本，不能假定历史状态仍有效。
本轮交接规格可能仍未提交；先保留并继承，不能直接切到旧干净分支导致规格丢失。
不直接在 master 开发；优先复用合适工作区，按工程护栏决定是否隔离，保护其他开发内容。
修改前报告已读文件、预计文件归属、必要验证和生产边界，然后继续已授权本地工作。

实施开发计划的阶段 0—5：
0. 核对真实契约、现有服务和能力缺口，不重复询问已确认业务。
1. 在岸岸能力包提供 Skill、Hermes Plugin 工具和 PMS 客户端，接可信请求人及查询。
2. 完成对话订房、收款登记、入住及已授权改期/续住/换房/取消/退房、恢复和人工接手。
3. 消费 PMS 支付 feed，做好首次基线、检查点、去重与可靠关联，复用阶段 2 的办理链。
4. 从飞书申请统一入口分派四老师与岸岸，保留原四老师职责，处理修订、乱序与补偿。
5. 接既有欢迎交接及工作约定驱动的跟进，完成分层验收材料和限定试点接线方案。
阶段 1、2 必须可独立交付，不等待收款事件、申请事件或新增中转服务。
完成后继续无外部阻碍的本地阶段 3—5；真实接线缺口明确记录，不拿模拟验证冒充上线。

架构与执行要求：
- PMS 操作归岸岸；复用 Agent OS 的人员、范围授权、队列、持久事项、确认和执行记录。
- PMS 保持订单、房态、库存和资金事实权威；不复制另一套 PMS，也不另建人员数据库。
- Skill 负责工作方法，受控工具负责鉴权与执行；不默认新增共享 Adapter 服务或 MCP Server。
- 岸岸现有 API Key 先核验主体与权限；凭据由代码读取 Profile .env 或既有安全位置。
  不让用户把 Secret 发到聊天，不输出密钥，不复制 default Profile；变量名以实际插件契约为准。
- 模型继续使用用户现有 Livecool Provider；真实 Profile、Bot、工具加载需分别核验。
- 操作者身份来自官方 WeCom/Gateway 可信上下文，不接受模型自填用户、批准人或 approved=true。
- 有效人类确认绑定业务方案，PMS 技术 Preview/Confirm 不等于要求人重复确认两次。
  参数、库存/预览或授权失效时重新核验；群聊可用名单不等于 PMS 写入授权。
- 复用 PMS 幂等键、command/receipt 和 UNKNOWN/resolve 协议；结果不明先查，不能换键重做。
  每个业务动作独立记结果，订房成功但收款失败不能整条流程重放。
- 保留客服直接用 PMS UI 接手。申请、付款、预订、实际到店分别核实；不能互相推定。
- 对话、收款、申请只在有可靠依据时汇合；同名、同金额、同一个人都不证明是同次住宿。
- 支付来自 Green PMS worker；申请来自飞书 Base，不让 PMS 产生申请事件或岸岸重复同步流水。
- 岸岸协调欢迎，阿靓制卡，二花审核传递或发送；不把制图迁入平台核心，不增加欢迎 UI 分类。
- 提醒按有效工作约定运行，完成后停止；缺参数不自动启动生产周期群催办。

Green PMS 参考仓库：/Users/feather/Documents/Codex project/Green PMS
飞书参考仓库：/Users/feather/Documents/Codex project/Feishu Base
先读各仓 AGENTS，默认只读复用契约。确有跨仓 API 缺口时说明最小差异及依据后另行落实。
本次授权范围是 Agent OS 本地实现、隔离测试、文档、必要提交及按项目规则准备 PR。
不自行修改 Evans CI、基础设施或部署流程；需要调整时提供具体差异、理由和复用价值供审阅。
本次不授权合并、发布、部署、服务器修改、真实 PMS 写入或对外发消息。

按 T01—T14 做必要测试，优先已有覆盖；使用隔离数据库和模拟外部边界，遵守 PMS 测试锁。
运行适用工程检查和 pnpm check:pr:auto，缺环境/真实集成不得算通过；不改门禁掩盖失败。
按可交付阶段提交代码并更新验收与交接报告；已获授权工作持续推进，不停在方案或重复问开工许可。
最终交付：业务演示入口、分支、最终 commit、PR（如已创建）、实际测试结果与剩余问题。
明确区分已实现、已自动验证、待人工验收、待生产接线和真实效果已验收。
```

## 首次文档交付记录与后续接手

首次交付只包含计划和提示词，以下为当时验证记录；后续以本文件的最新调度基线与实际接手证据为准。文档不证明插件已安装、Token 已绑定或任何真实 PMS 操作已成功。生产试点另按服务器政策及具体授权推进。

2026-09-24 文档交付核验：

- 变更文档 Prettier、全库 `pnpm lint:md`（443 份文档）及 `git diff --check` 通过。
- 8 份相关文档的 150 个本地链接文件目标均存在。
- `QINTOPIA_LOCAL_PR_BASE_REF=HEAD pnpm check:pr:auto` 通过，自动选择 quick
  tier，包含完整
  `check:light`。使用现有基线参数评估当前未提交文档增量；不代表历史分支相对主线的完整 Rust/PostgreSQL 验证。
- 本轮未改变 CI、运行时代码或生产配置；文档保存在当前工作区，未提交。原有未提交规格保留。

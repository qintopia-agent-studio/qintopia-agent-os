# Agent OS 跨项目开发协调

日期：2026-09-24。负责人要求总指挥同时跟进两条现有任务，协调接口、修改范围和交付顺序。具体实现留在开发任务；总指挥维护共同决定、依赖及验收边界，不要求用户转述任务消息。

## 2026-09-25 当前生产接手决定（优先于下文历史收尾快照）

PR #721 已合并为 `470c4505`；当前接手现有 Draft PR #722，接手时远端 head 为
`d5a11ba5`。本地生产接线仍在实施，旧 #721 归属失败不再是当前阻断，不重启已暂停的收尾自动化。

负责人最新确认：Green PMS
worker 的新收款主动通知、申请接入、提醒及欢迎都纳入上线目标。这取代 #722 接手提示中“首批暂不启用”的阶段范围，不意味着已部署或可以跳过真实操作确认。

收款事件按既有契约先送 Agent OS Sidecar 的
`POST /api/v1/ingress/pms/events`，验签、校验来源与物业、去重并持久保存后返回接收回执，再沿共享 WorkItem 分派或唤醒岸岸。PMS 不直接调用岸岸模型；

岸岸负责查事实、沟通建议、有权人逐笔确认、办理和回读。feed 用于首次基线与漏失补拉，不能代替主动推送；收到事件和收到消息均不等于已登记收款。

总指挥已再次交叉核对 PMS
worker 的固定路径及 Sidecar 的事务回执。岸岸任务已提交官方 Generic
webhook 的只读接线调查；它只可能承接 Sidecar 提交后的内部唤醒，不能将 PMS 绕过 Sidecar。官方 202 与进程内去重不构成持久通知或人类送达，实际只读工具集和网关命令边界仍在核查。

申请继续来自飞书统一 Bridge；工作提醒依有效规则；欢迎保留岸岸协调、阿靓制卡、二花按本栋规则处理，四老师辅助主管、小满负责活动。

候选生产代码仍有尚未完成的 live 接线；旧客户端人工范围限制已移除，服务端授权与能力门禁保留，不能将接线缺口写成用户要求关闭。

基础任务负责同一 7243 树内共享 live 身份/授权、可选 broker 与受信运维配置 CLI；本任务负责插件/隔离和发布准备。

申请、私有投影、逐人手机号与 socket 恢复切片的最终原始证据及 12 项源码校验已核对，旧失败记录保留。提交后内部唤醒已交接并冻结 Rust；总指挥核对最终源码后接手适用项目检查。

岸岸只读 webhook 工具已统一 `work_item_id`
契约；独立 Profile 的默认 route 冲突已修，官方实际 HTTP/hook
8 项与 PMS 默认套件 88 项模拟通过。Sidecar 提交后 sender 已完成，实际 listener、官方 hook 与 Unix
broker 联合模拟通过。

安装与隔离前提由该联合场景明确替代，不是生产验收。

普通人员 CLI 与宿主来源观察候选已完成：未知账号仅建 pending，不假冒 NATS 鉴权，不因观察而授予业务权限。完整 Linux
Gateway 的
`production.require()`、真实插件发现及 Docker 工具隔离模拟已通过；发现官方浏览器读取 raw
Profile，已要求与 managed 配置一致，否则拒绝。该模拟 UID 为 0，未替代生产用户/配置验收。

新账号将只通过可信宿主观察产生 pending 来源，再由共享配置明确确认身份及单独授予业务权限。首位管理员引导未定，不自授权。浏览器真实容器文件系统和 Linux 受管目录权限模拟已通过；完整生产隔离仍待验收。

不重新开发或重建已收尾的 PMS 工作树，不并行覆盖公共文件。

官方工具隔离候选中的宿主 cron 脚本/browser_use
Python 限定仍待负责人决定；与业务主动通知是否启用分别处理。当前只读确认 PMS 版本 v1.7.2，未证明新 worker 已部署。实现与验证细节见
[生产接入计划](anan-production-rollout.md)及[接线记录](../../reports/2026-09-25-anan-production-integration.md)。

最新发送端四项源码 SHA256、Cargo
check 与严格全特性 Clippy 已核验一致，模拟库为本任务独占的
`agentos-anan-payment-read-20260925` /
`127.0.0.1:55498/qintopia_test`。PMS 包 89 项、Workflow
24 项和完整 Linux 隔离模拟通过；联合 webhook/broker 单项通过。

独立复核指出 MCP 动态配置可能先启动宿主进程，官方插件加载失败亦会继续运行；调用时检查不等于进程隔离，固定插件与配置的生产启动保护仍待完成。现有业务测试清单补登 8 个已有 Rust 场景，沿用原执行器与 exact 名称，未改 CI/发布门禁。

候选验证增量：申请场景的环境串扰已按原包内子进程模式修复，最终共享协作整组 146 项、欢迎整组 24 项通过；修复源码及原始日志哈希已核验。最后 apply
smoke 在本任务 55498 被既有 URL 哈希白名单拒绝，本地完整 auto 不记为通过；不改 CI/白名单、不使用他人 5432。后续核对本批新提交的原 CI 隔离库结果。

## 2026-09-25 当前交付与 PR 收尾

本节更新下方早期阶段记录，不把历史“待实现”解释为当前缺口。本批三线本地实现和共同验证已完成，尚未上线。

- Agent OS 唯一集成 PR 为
  [#721](https://github.com/qintopia-agent-studio/qintopia-agent-os/pull/721)，首轮 head
  `0414b23` 已包含基础线 `85cf413`，共同代码为
  `69b054d`。基础线只读兼容审阅未发现阻断服务集成的缺陷，不另开重复 PR。
- Green PMS 功能 PR #51 已 squash 合入 `e7f7716`，文档 PR
  #53 已由总指挥核对并 squash 合入 `0dbb320`，合并后适用检查通过；PMS `e05d`
  工作树已由总指挥非强制移除并核对目录与注册消失，原分支及其他工作树保留。旧支付联合场景不重放。
- 用户已授权提交、推送、普通 PR 审查及总指挥核对后合并，再归档停用模拟服务。该授权不包含 Release
  PR、发布、部署、真实业务写入、真实外发或扩大 CI/allowlist。
- #721 首轮 Light 因新 PMS 包缺少生产归属失败，与本地 apply-smoke
  allowlist 失败分开记录。原 smoke 有 CI 独立容器获准路径，以精确 head 结果验收；不预报通过。最新审查与门禁详见
  [PR 集成报告](../../reports/2026-09-25-pr721-integration-review.md)。
- 归档在根仓库受限
  `.local-workspace/archives/agent-os-pr-closeout-20260925/`，包含必要日志、模拟库逻辑备份、配置、校验和与恢复说明，凭据不入 Git。

只停自己已确认不再被验证依赖的模拟服务。基础线已归档并正常停止旧服务。

- 基础线工作树由总指挥统一收尾，PMS 工作树已完成清理；岸岸集成工作树保留至首轮线上验收。不删除主目录、历史分支、其他任务资料或将根脏文档整份覆盖进集成树。

来源表单手机号已只读核实必填且可见；实际空号提交、真实模型/渠道/部署仍待外部验收。最终共同 Unix 断连、PG 宿主认证、contacts 模块及严格 Clippy、quick 证据见集成报告；不同层级重叠不累计，原失败记录保留。

## 2026-09-24 早期任务与阶段状态

### 本体基础与欢迎闭环

- 任务 ID：`01a0c959-5c49-72e3-8de1-5711e06fd964`，host 为 `local`。
- PR #720 已合并，merge 为 `b42db0d8b8388c65dcf62c1f1fd41a7325298c5e`；原最终 head 为
  `a128cf989f673d5655e8f44e8af9b1c8f3c3e2de`。
- 开发任务回报原批次完整本地检查及远端 8 项检查通过；旧 PR 完成不代表真实欢迎业务已验收。
- 当前在需求收尾及用户授权的只读身份关联审计，尚无新批次版本化业务源码、迁移或提交。根工作区仍为
  `codex/person-rule-lifecycle@3030d67`，保留总指挥及其他任务的未提交资料。
- 临时审计证据在忽略目录
  `.local-workspace/identity-link-audit-20260924/`；任务回报已检查 499 名群成员的字段结构，自动身份关联成功率尚无结论。本文不保存成员原文或凭据。
- 任务回报具备现有数据与接口的只读审计授权；其来源与具体执行证据由该任务记录。本协调不额外授权生产写入、自动关联落库或发送。

### 岸岸 PMS 开发

- 任务 ID：`01a0d2c3-4d3e-7f72-9206-fce386254bef`，host 为 `local`。
- 工作区：`/Users/feather/.codex/worktrees/7243/qintopia-agent-os`；分支
  `codex/anan-pms-collaboration`。
- 从 #720 的 merge 创建，继承规格提交为 `adff875d829c70b1fa938318505bb3f6b81824bc`。
- 在实现 PMS 对话查询、办理、持久确认、恢复与后续事件消费者。完整本地办理链已获模拟身份和真实本地服务的验证；真实模型、企微渠道及生产尚未验收。
- 继续补齐方案变化、精确委派、改期/续住/缩住/换房及人工接手后的恢复验证，并准备阶段提交。详细结果由该任务的交付报告维护，总指挥仅核对并记录摘要。

以上为当次任务回报与可读日志的状态，不替代后续实际 Git、测试和交接结果。

## 分工与共同接口

| 范围                                                                | 主要负责方               | 协作要求                                                                      |
| ------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------- |
| 共同 Person、可信身份关联、人员姓名与渠道昵称展示、岗位选择         | 本体基础与欢迎闭环       | 先完成只读审计；不按同名、头像或同群直接认人，不增加工作人员逐人录入负担      |
| 欢迎内容/资格确认的群与 UI 接线                                     | 本体基础与欢迎闭环       | 同一持久事项、同一状态与权限依据；不复制 PMS 操作确认                         |
| PMS 查询、方案、业务确认、执行及原键恢复                            | 岸岸                     | `skills/pms-operations/`、`agents/anan/` 与专属共享增量；复用共同身份和授权   |
| 申请/收款事件与住宿事项关联                                         | 岸岸                     | 可靠关联后接现有欢迎入口；不另建身份库、制卡/发送流程或欢迎 UI                |
| `person_collaboration` 公共 model/store/mod、`foundation_server.rs` | 按语义拆分，由总指挥协调 | 共同身份语义由基础线维护，PMS action/路由由岸岸维护；小增量提交，不整文件覆盖 |
| registry、迁移、共用检查、发布边界                                  | 总指挥协调具体修改者     | 按已有专项批准与契约执行；新增冲突先列差异，其他工作继续                      |

岸岸预留 `202609240001_business_operation_execution.sql`，以及
`business.rs`、`business_events.rs`
的业务执行增量。基础线目前无新迁移；后续先核对全部活跃分支和已应用记录。

旧前台草稿 `202609230008_frontdesk_interactions.sql`
与主线编号碰撞尚须在其整合时处理，不能混进这两条线偷偷重命名已应用迁移。

## 新决定的衔接与待明确项

“本体基础与欢迎闭环”任务回报以下最新用户决定，已要求其写入专项报告并附可定位来源：岸岸直接在“全村人运营团队”呈现卡片和文案、发起客房确认，不经四老师转发；

可在 UI 配置有权确认者与目标群，群与 UI 使用同一事项；客服和主管应能在有效权限内补位。

协调解释如下；若原用户决定另有含义，凭来源回总指挥核对，不由任一线自行扩大：

- 欢迎卡片/文案及资格确认与 PMS 订房、收款、入住确认分别绑定各自的操作效果和权限。前者的确认不能代替后者。
- 阿靓负责制卡；二花依本栋规则承担后续审核传递或欢迎发布。岸岸向运营团队呈现内容，不自动改变最终欢迎发布者。
- 四老师仍辅助主管管理规则、例外和协调，无需成为每次客房确认的转发环节。
- 目标群必须绑定可靠渠道标识；成员在群中或拥有主管/客服名称不自动取得业务执行权。补位依据当前职责、物业/楼栋范围及精确操作授权。
- 用户已明确“秦托邦小客服”可在本轮欢迎确认中作为指定可信工作账号主体，不追溯每班自然人。不按昵称认人或自动赋权，账号主体须诚实标注；共同身份契约由基础线实现，不伪造自然人 Person。

此决定不扩大 PMS、资金或其他业务权限；每笔收款仍需要对具体订单、流水和金额明确确认。

- 照片和欢迎卡片统一，楼栋欢迎词优先采用本栋舍长措辞；统一资料/卡片与楼栋措辞分别保存版本。仅措辞调整不重做客房事实确认仍为实施建议，不豁免楼栋审批；事实或披露内容变化须按影响复核。
- 面向使用者显示姓名、渠道昵称等可理解的信息，避免用内部 Person
  ID 要求人辨识。中文测试描述统一使用“模拟”。

### 申请与身份关联补充决定

保留飞书申请；系统用手机号、姓名、昵称与 PMS 逐个实际入住人生成候选，已有可靠链接可复用。

不确定时由岸岸在“全村人运营团队”集中请可配置的“秦托邦小客服”工作账号或有权主管确认，UI 使用同一事项。必须明确渠道账号、人员/申请、每个实际入住人之间哪段关联获确认；

缺账号或证据保持待关联，模糊分数或单一候选不自动认人。

确认后由共同服务保存 Agent
OS 来源关联、确认主体和版本，不修改 PMS 住宿事实或飞书源字段。身份关联确认与卡片内容确认分别记录效果，可以同一事项呈现，不混用批准。

不默认二花可以主动私聊住客；自助私聊仅在渠道可达时作为可选补充。

共同主体、授权、关联服务及欢迎入口仍由基础线维护；岸岸只提供住宿、申请、WorkItem 引用及接线。工作账号的欢迎关联权限不扩大 PMS 权限，每笔收款仍须有权人确认具体流水、金额和订单。

## 基线与整合顺序

1. 两条线持续做已获授权且无冲突的工作。当前身份审计不阻塞岸岸对话办理的阶段提交。
2. 基础线若进入新一轮已授权源码实施，先核对最新主线，在合适的隔离 checkout 中开发。根工作区保留总指挥资料；不从旧
   `3030d67` 直接叠加新业务。按工程护栏优先复用合适工作区，保存原验收证据。
3. 岸岸先收口可独立验收的 PMS 能力与最小共享扩展，基础线提交身份/欢迎接口的清晰增量。公共身份或宿主协议若确有依赖，先提交接口提供方，再由调用方整合；不预先按任务名称强制合并先后。
4. 合并前比对双方公共文件、schema 与迁移，运行受影响的共同身份、授权、欢迎及 PMS 回归。分别通过不代表整合版本通过；无需无理由反复跑所有无关测试。
5. 欢迎内容确认与 PMS 业务确认在最终整合版本验证不混用权限、不重复催确认或发送，停权、改方案、UNKNOWN 和人工接手后状态一致。

合并、发布、部署及真实业务操作仍受各自有效授权约束；本协调不是上述动作的批准。

## 已批准与等待决定的范围

- A：身份登记与受管部署分离的最小通用调整已批准，由岸岸实施；不包括 resolver、restart
  rules、安装 payload 或生产接管。
- [PMS 事件头只读接口](anan-payment-feed-baseline-proposal.md)：首次 head 方案已获用户批准；源接口与主动通知由 Green
  PMS 项目负责。本线只读参考源码，在 Agent OS 内实现接收与补拉，不跨仓修改。
- [岸岸开发交接](anan-pms-development-handoff.md)及[架构评审](../../reports/2026-09-24-anan-architecture-parallel-review.md)保留完整边界。

## 跟进方式

已向两任务发送当前分工，要求阶段交付、共享语义变更和冲突主动回报总指挥。文件归属为本次协调安排；具体接口仍以双方提交及承接记录为准。

岸岸任务已回报承接：不新增欢迎确认 UI 或身份模型，仅提供住宿事实、申请来源引用和既有 WorkItem 关联；等待基础线明确公共入口。欢迎确认与 PMS 授权保持独立，新增宿主证据路径限制在岸岸，保持既有其他 Agent 路径兼容。

本对话已启用“Agent OS 双线开发统筹”跟进，automation ID 为
`agent-os-2`，每 15 分钟核对两任务。优先读取紧凑状态；仅有实质变化时深入核验证据并通知用户。无变化不重复汇报，未变的待批准事项不反复催问。

旧 `agent-os`
是另一个任务中已暂停的阶段就绪检查，保持暂停。双方约定成果及交接完成后汇总并暂停新的跟进，不以本地通过宣称生产可用。

# 组织与人员工作台 A：增量实施

日期：2026-09-11。承接 person-agent-foundation-parallel-handoff 的 A，不改变共同业务契约。

已有人员、任职、职责、工作连接、三态授权、事务预览和审计继续复用。新增组织岗位归属、台账生命周期与面对对象配置，页面调整为组织关系、配置任职与范围、基础台账。组织上级只是责任结构，不能自动产生授权；登记、恢复台账不能恢复已结束的任职。

先交付 B 提出的最小接收器缺口：非默认 `welcome-synthetic-driver`
feature 下的单步补偿 CLI；仅在显式开关、literal loopback、qintopia_test 和 synthetic
source 下运行。GET 客户端保留真实协议解析与有界 HTTP；本地 HTTP 传输只允许固定 literal
loopback 根地址，不跟随重定向。生产构造函数仍强制 HTTPS。初始化不覆盖既有 source，不开启外部执行。

冻结基线在独立临时目录保留逐文件 SHA256，B 复制运行。共享入口的最终变化须再通知 B 回归。迁移只在专用合成库执行；全部外部制卡、上传、PMS 写和发送保持关闭。

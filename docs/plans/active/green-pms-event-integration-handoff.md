# Green PMS 事件集成：交接与实施核对清单

日期：2026-09-09。交接状态：已准备，未向其他任务发送，未开发。接收项目：原 Green
PMS 项目及其现有任务。

共同契约的唯一维护入口是
[统一人员与入群欢迎 V1](unified-person-welcome-v1-contract.md)第 3 节；

身份/欢迎背景见[评估报告](unified-person-identity-foundation-assessment.md)。

本文件限定 PMS 的代码责任和核对结果，不复制一份容易漂移的协议。

原项目请先读取自己的 AGENTS.md、相关 A 级实施规格和已有验收计划，继续保留原业务与发布约束。

## 1. 已确定的范围

Green
PMS 负责已提交住宿事实的可靠事件来源：事务 Outbox、固定订阅方 webhook、签名/重试/重放、事件变化读取与最小住宿投影。Agent
OS 负责接收器/Inbox、人员关联、角色审批、工作项和欢迎。

采用 webhook 主触发、轮询对账兜底；不是二选一。共同契约已给出候选路径、字段、错误码、签名串、发布水位与重建规则，请直接核对可行性，避免再做一轮泛化选型。

不新增平行 Person 库，不把二花/阿靓/欢迎文案或楼栋群配置写入 PMS。

首个欢迎闭环只读 PMS，不开放 Agent 自动 CREATE_ORDER/CHECK_IN 等写命令；

工作人员继续在 PMS 办理真实业务。

固定等待一小时已取消，但这不改变 PMS 的实际入住语义。

## 2. 源码落点与复用依据

根目录为
`/Users/feather/Documents/Codex project/Green PMS`。以下为已读源码定位，不是要求修改所有列出的文件，更不是生产已部署证明。

| 文件/范围                                | 可复用内容                                                                      | 后续实施工作                                                                                 |
| ---------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| apps/api/src/server.ts、schemas.ts       | /api/v1 路由、结构校验、OpenAPI 和认证边界                                      | 按共同契约增加事件读取与最小投影 schema/路由；返回权限/游标错误语义                          |
| apps/api/src/auth.ts                     | Bearer、主体权限、READ/WRITE ceiling、物业 scope、到期/撤销                     | 复用最小 READ 对事件/投影查询授权，订阅配置/重放操作另走受控管理面，不默认扩大 READ 为写权限 |
| packages/contracts/src/                  | 命令枚举、入住/安排/occupant 等契约                                             | 事件类型/DTO 与版本约定；与源命令效果对照，不复制 Agent OS 人员模型                          |
| packages/db/src/commands/service.ts      | 同事务应用业务效果、回执、审计、幂等执行                                        | 在权威提交事务内产生 Outbox；外部 HTTP 不在该事务内调用                                      |
| packages/db/src/orders.ts                | appendAmendment、sequence/prior/new_version、生命周期投影                       | 复用可证明完整的聚合版本及最小事实引用，历史修正保持原语义                                   |
| packages/db/src/schema.ts 及既有迁移体系 | orders、stays、segments、occupants、inventory_units、member_external_references | 后续按项目规范设计追加结构/索引、必要集成修订、Outbox/发布水位；本轮禁止创建迁移             |
| packages/db/src/order-list.ts            | 创建时间/ID 分页和查询限制                                                      | 原接口保持兼容；created_at 分页不是增量事件 cursor，不悄悄改变原 UI 语义                     |
| packages/db/src/members.ts               | FEISHU_BASE 记录外部引用读取                                                    | 复用现有申请来源关联，投影过滤掉身份明文；不把 member_id 当实际 occupant_id                  |
| 后台纠正、目录/会员管理等写路径          | 可能在核心命令入口以外改变当前人员/住宿上下文                                   | 列覆盖表；统一产生变化或明确走独立上下文事件，不能只接常见五个命令                           |

## 3. 需要原任务核对并返回的技术结果

| 编号 | 核对问题                                                       | 期望返回                                                                                |
| ---- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| P-01 | 每种相关命令/后台写入何时成为已提交事实                        | 路径 → event_type / aggregate / revision / origin 的覆盖表；无事件者给出理由            |
| P-02 | order/member/inventory 的 version 是否覆盖所有会影响投影的变化 | 可复用的版本与需补集成修订的对象；不可用时间戳或 hash 冒充单调版本                      |
| P-03 | Outbox 与业务提交是否同事务，后续发布序号如何产生              | 事务边界和唯一键；先取 ID 后晚提交、进程崩溃、重试及多 Worker 的不漏事件证明            |
| P-04 | 事件 feed 和 webhook 是否读取同一不可变信封                    | 同 event_id/body，逐物业不透明 cursor、水位/过期、page commit 和重放语义                |
| P-05 | 最小投影能否返回真实有效安排与实际入住人引用                   | 字段映射/权限/墓碑，避免回传姓名、电话、证件、资金或完整回执；未知床位不按 ordinal 推断 |
| P-06 | 验证/重试/暂停/死信/重放/轮换如何配置                          | 使用共同签名/ACK 表，配置固定订阅方；明确审计与恢复动作，无任意 URL 或重定向            |
| P-07 | 现有发布时间/生效时间对历史补录的表示                          | recorded_at/effective_at/origin 定义及测试；不能把历史修正包装成 live 新住客            |
| P-08 | 既有部署/测试链能否承载新 Worker 和状态                        | 分阶段开关、容量/清理、回滚和必要回归；不自动部署                                       |

接受共同契约的条目可直接写“兼容”；不兼容的只返回“条款 → 源码证据 → 影响 → 最小调整建议”，不要把已确认的产品决策再交回负责人猜测。出现接口路径/字段/语义变化，应先回传 Agent
OS 更新同一契约版本，再分头实现。

## 4. PMS 实施切片（未来取得开发授权后）

| 切片                 | 范围                                                 | 验收                                                                       |
| -------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------- |
| P1 事务事件基础      | Outbox、聚合版本、事实覆盖、已提交发布序号           | preview/回滚无事件；同命令幂等回放不重复事件；并发晚提交仍可读到           |
| P2 读取/补偿接口     | 同一事件 feed、分页/游标过期、所有状态最小投影和墓碑 | 物业权限、同时间多变化、扫描期间新增/取消/删除、基线追赶最终收敛           |
| P3 固定 webhook 投递 | 签名、受控 endpoint、退避、ACK 校验、死信/重放/轮换  | ACK 丢失安全重试；403/契约错误可定位并暂停；外部宕机不拖住 PMS 业务事务    |
| P4 发布准备          | 配置分离、指标、保留窗口、容量和恢复 runbook         | 未激活不投递，固定测试接收方验收；清理不删除未处理死信；回滚不破坏业务事实 |

P1/P2 可与 Agent
OS 身份/Inbox/客户端切片并行，使用同一组合成数据。不要在两个 Codex 任务中同时修改 Green
PMS 同一模块。完整客房 Agent 和真实住客群不是 PMS 接口开发/测试前置条件。

建议工作量沿用评估口径：可靠固定 webhook 基础约 5–10 人日；稳健变化读取/扫描约 3–6 人日，若共享发布日志/查询实现则重估重叠；联合故障验收另计。原任务应根据 P-01/P-02 的真实覆盖调整，不把估算当固定承诺。

## 5. 测试与发布边界

复用
`tests/contract/openapi.contract.test.ts`、`command-effects.contract.test.ts`、`agent-core-journey.contract.test.ts`
和既有订单生命周期、occupant 更正、命令/权限、数据库测试锁机制。新增测试优先覆盖共同契约 T07–T11 和 P-01 写路径，不写只复述实现的断言。

未来验证入口：`npm run verify`，通过项目现有 runner 执行 `npm run test:integration` 与
`npm run test:contract`
中的相关范围；事务/权限/库存受影响时扩大相应回归。不得绕过数据库测试锁，所有测试只用明确的隔离测试库和本地接收器，不能调用真实 webhook、真实成员或生产 PMS 写入。本交接阶段不运行这些会写测试库的命令。

重点故障：业务事务失败、写 Outbox 后提交失败、发布后投递前崩溃、ACK 丢失、一个旧事务晚提交、签名失效、订阅方 24 小时不可用、cursor 超期、最小投影权限撤销、后台纠正未覆盖，以及 feed/webhook 双路重复。

生产 rollout 需先验版本/授权范围，部署兼容结构及禁用状态的发布/投递 Worker；再经发布流程在固定测试接收方验证，最后按批准的物业/订阅配置激活。Outbox 捕获的启用须与基线和发送准入边界对齐，历史初始化不标 live。

回滚先停投递/消费激活，保留未发事件、游标和死信，PMS 原业务继续；读接口/结构优先向后兼容，不靠删除历史事件或回滚住宿事实恢复。若存在已经发出的 HTTP 请求，以相同 event_id 重放并由 Agent
OS 去重；不让旧新两个投递器以不同 ID 表达同一事实。

## 6. 可直接粘贴到原 Green PMS 任务的提示词

> 请在本 Green PMS 项目继续完成事件集成的实施核对，目前只做只读分析和文档，不开始开发。
>
> 先读本项目 AGENTS.md 和相关 A 级规格，再读以下两份已准备文件：
>
> - `/Users/feather/Documents/Codex project/qintopia-agent-os/docs/plans/active/unified-person-welcome-v1-contract.md`
> - `/Users/feather/Documents/Codex project/qintopia-agent-os/docs/plans/active/green-pms-event-integration-handoff.md`
>
> 已确定：PMS 固定 webhook 主触发、轮询对账兜底；复用现有事务、amendments、版本和回执。PMS 只提供住宿事实/关联引用/可靠事件，Agent
> OS 负责接收器、统一 Person、Inbox、审批/WorkItem 与欢迎。无需重新盘点业务方案，不建立平行人员库，不实现欢迎或群路由。
>
> 请按交接清单 P-01 至 P-08 核对本仓源码，确认共同契约可实现性，列出全部相关写路径、版本覆盖与唯一键、Outbox/发布水位的事务方案、最小投影/墓碑、测试、发布回滚及修订工作量。相容条款直接复用，差异只返回具体条款、证据、影响和最小调整建议。共同协议只维护一份，先回传差异，不擅自修改兄弟仓库或让两边分别发明新协议。
>
> 可在本项目既有规格/计划体系保存 PMS 侧核对文档。不得修改代码、创建迁移、执行 PMS/数据库写操作、改变 Base/Workflow/Hermes/生产配置，或发任何外部消息；不输出、保存或提交凭证、手机号、证件、真实群 ID、原始住客/聊天/完整日志。服务端仅只读。文档完成后停止，等待我明确“开始开发”。

## 7. 回传完成标准

一份可审查的 PMS 技术核对结果即可：共同契约兼容/差异表、写路径事件覆盖、P1–P4 文件范围/测试和部署边界。阻塞要写明具体来源、缺什么、影响哪个切片；不把“尚未建立客房智能体”列为 PMS 事件接口阻塞。

本清单已可交给原任务作规格核对，尚不代表该任务已接收、已批准共同契约或已开始实现。

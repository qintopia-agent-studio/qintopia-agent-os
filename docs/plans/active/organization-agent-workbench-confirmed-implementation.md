# 组织与智能体工作台：确认稿实现交接

日期：2026-09-11。范围：交接文档 §2、§3.1；§3 为已执行历史，不重复实施。

## 基线与修改责任

- 原任务“实现统一人员与欢迎 V1”经 Codex 任务状态只读核对为 idle，最近一轮 completed。
- A 冻结来源 `/private/tmp/agentos-a-baseline-20260911-v1` 的 `BASELINE.json`
  共 162 文件，逐文件 SHA256 全部匹配。
- 当前工作目录对应文件与快照的差别仅为两份计划：
  `person-agent-foundation-execution-plan.md`、
  `person-agent-foundation-parallel-handoff.md`。本工作区采用这两份最新指令。
- 独立工作区：`/private/tmp/agentos-org-workbench-confirmed-20260911`；分支
  `codex/org-agent-workbench-confirmed`，HEAD `2815ab46f2cc0efb3e34cda56f6242bb9af5abf0`
  加上述冻结源码。
- 冻结清单保存在工作区根
  `WORKBENCH-BASELINE.json`；它记录导入来源，不代表新实现已经验收。
- 本任务单独负责该工作区中
  `person_collaboration/workbench*`、其本地 HTTP接口适配及必要的模块内命令/事务修复、对应测试和文档。原工作目录、PMS协议、欢迎接收器、共享入口及迁移不在默认修改范围。
- 后续合入需按本任务增量文件对照基线，不把导入的未提交后端当作本任务新改动。

## 确认稿与组件映射

已读取原稿全部源码，原稿不修改。SHA256：
`82f55877b8d7bded8c2097ee62f97b2053d7be44790ab46a62e5415b290ae4b0`。

| 确认稿组件                                | 现有服务                                                                 | 实际适配与待验收项                                                             |
| ----------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| 三个主导航、245px 组织树及右侧详情        | `GET /api/state` 的 organization.positions、relations、grants、audiences | 用真实上下级与空缺构建树；管理权从有效授权说明，不由上级连线推断               |
| 管理关系、触达范围、决定权三个详情面板    | state、`POST /api/decision`、`POST /api/contact-decision`                | 将当前技术连接视图重组为原稿层级；未接入状态与有效配置分别表达                 |
| 四组任职表单、内联影响预览                | `POST /api/preview`、`POST /api/save`；ConfigureWork                     | 已将任职、权限及触达合并为单事务；触达失败整笔回滚，保留旧命令兼容入口         |
| 姓名/昵称与任期                           | people、organization.ledger、Assignment.valid_until                      | 沿用稳定身份和消歧；待核验人员不可任命；补齐确认人、期限与代理的渐进展开       |
| 智能体、群、人员、岗位四类台账的列表/详情 | SaveLedger、SavePosition、SaveRole、SaveDuty、范围与群绑定命令           | 保留四类主要入口；岗位职责与范围定义在相应详情内维护；不丢弃原有业务语义       |
| 解除、换人、停用/归档与恢复               | EndAppointment、EndCollaboration、Lifecycle                              | 复用事务预览、立即撤权、历史保留与恢复不自动赋权；关联数与影响以服务端结果为准 |
| 保存、失败和并发恢复                      | Command.operation_id、expected_version、命令历史                         | 保留幂等编号重试；版本冲突需重新读取与预览；禁止把请求发出当保存成功           |

视觉固定采用原稿深色参数：背景 `#19201c`、面板 `#242c27`、文字 `#e9efe9`、辅助文字
`#a9bbae`、边框 `#3b4c3f`、强调 `#a0d8b6`、选中 `#304735`、主按钮文字 `#193523`、提示
`#eac583`。保留原稿 14px/1.55 正文、22px 标题、22px 页面留白、10px 面板圆角、830px 表单上限、四组表单及台账列表/详情比例；补齐业务状态时不改选其他风格。

## 验证与本地边界

已完成 JavaScript 语法、模块专项与隔离 Postgres 事务测试（21 项）、all-features 测试（724 项通过、47 项按原门禁忽略）和两组 Clippy。具体命令、浏览器案例及最终检查见验收报告。

成品已逐页浏览，完成窄屏与键盘路径；合成库验证刷新/重启持久性、同名、换任撤权、跨栋隔离、版本冲突恢复和停用影响。使用独立端口与新 synthetic
tenant，不重置 A/B 的测试数据。原稿逐页对照与浅色系统偏好模拟仍待验，不将源码规则核验写成实际浏览器对照通过。

不部署、不改生产、不发外部消息、不接入真实账号或 PMS 写操作。真实登录、渠道身份、runtime 配置消费与欢迎业务仍属于后续切片。

## 当前状态与阻塞

已完成基线核验、独立工作区导入、源码组件/接口映射及本地实现。按主项目新增的浏览器阻塞处理完成独立工作：重建六个
`workbench*` 文件，增加 `ConfigureWork`
原子命令及前后快照，沿用既有事务与权限服务；没有新增迁移或修改 PMS 协议。

本地体验服务为 `18875`，使用独立合成 tenant；无管理权验收为临时
`18876`。新工作台自身可以正常浏览器访问。实施与验收记录见
[本轮报告](../../reports/2026-09-11-organization-agent-workbench-confirmed.md)。

浏览器工具拒绝访问确认稿的 `file://`
URL，明确要求不得通过其他浏览器、间接执行或其他方式绕过该 URL 策略。

主项目 §3.1 随后明确：原稿对照不可用时单列待验，继续独立实现和成品验收，不要求用户寻找预览地址。此前因此停止整项工作的处理已纠正；原稿逐页浏览与对照仍未完成，不以源码阅读或旧 A 验收代替。

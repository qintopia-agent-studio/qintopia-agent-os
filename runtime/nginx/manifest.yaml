id: runtime/nginx
type: runtime
name: nginx Ingress Templates
owner: PatrickLiveCool
status: draft
risk_level: high
description:
  Default-disabled webhook and API ingress templates, including the owner-gated Qiwe
  authenticated callback scaffold and fixed rollback boundary.
source:
  path: docs/operations/runtime-baseline.md
  reference: current-monorepo
  disposition: template
production_boundary:
  external_sends: true
  database_writes: false
  runtime_profile: false
  secrets: true
validation:
  commands:
    - pnpm runtime:nginx:check
dependencies:
  - deploy/sidecar
tags:
  - runtime
  - nginx
  - ingress
next_actions:
  - Complete the one-time reviewed disabled include and server-local secret bootstrap.
  - Use the fixed owner-gated apply target only after adapter authentication is live.
  - Capture one sanitized real Qiwe callback in shadow before enabling event mappings.
  - Configure the real Qiwe callback with reviewed official endpoint semantics; the
    repository intentionally does not invent provider API parameters.

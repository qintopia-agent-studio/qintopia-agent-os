id: runtime/postgres
type: runtime
name: Agent OS Postgres Runtime
owner: TBD
status: adopting
risk_level: high
description: Migration and schema contract for the Agent OS system fact source.
source:
  path: ../qintopia-message-sidecar/migrations
  reference: eda2652f21999e4f32699463413372accbd3b76e
  disposition: adopt
production_boundary:
  external_sends: false
  database_writes: true
  runtime_profile: false
  secrets: true
validation:
  commands:
    - cd ../qintopia-message-sidecar && cargo test
    - cd ../qintopia-message-sidecar && scripts/operations-control-plane-smoke.sh
dependencies: []
tags:
  - postgres
  - migrations
  - fact-source
next_actions:
  - Import migrations with matching data-design notes.
  - Add migration inventory validation before server cutover.

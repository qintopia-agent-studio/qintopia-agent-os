id: runtime/sidecar
type: runtime
name: Qintopia Message Sidecar Runtime
owner: TBD
status: adopting
risk_level: high
description:
  Rust service contract for Agent OS message capture, workers, and runtime checks.
source:
  path: runtime/sidecar
  reference: eda2652f21999e4f32699463413372accbd3b76e
  disposition: adopt
production_boundary:
  external_sends: false
  database_writes: true
  runtime_profile: false
  secrets: true
validation:
  commands:
    - cargo fmt --check --manifest-path runtime/sidecar/Cargo.toml
    - cargo test --manifest-path runtime/sidecar/Cargo.toml
    - cargo check --manifest-path runtime/sidecar/Cargo.toml
    - pnpm smoke:sidecar
dependencies:
  - runtime/postgres
  - mcp/context-server
  - mcp/message-store
tags:
  - rust
  - nats
  - worker
  - message-capture
next_actions:
  - Add monorepo smoke wrappers for no-credential sidecar and operations checks.
  - Reconcile local main with the server Huabaosi shadow branch before adopting shadow
    files.

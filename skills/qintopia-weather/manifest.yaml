id: skills/qintopia-weather
type: skill
name: Qintopia Weather
owner: PatrickLiveCool
status: adopting
risk_level: medium
description:
  Standalone fixed-location Qintopia weather capability for current conditions, umbrella
  windows, thunderstorm windows, official alert evidence, and air quality.
source:
  path: skills/qintopia-weather/__init__.py
  reference: qintopia_weather_lookup
  disposition: adopt
production_boundary:
  external_sends: false
  database_writes: false
  runtime_profile: true
  secrets: true
validation:
  commands:
    - pnpm skills:qintopia-weather:check
    - pnpm skills:qintopia-tools:check
    - pnpm mcp:adapters:check
dependencies:
  - mcp/weather-provider
tags:
  - skill
  - weather
  - qweather
  - fixed-location
next_actions:
  - Move provider-specific QWeather and Open-Meteo calls behind mcp/weather-provider
    when the provider adapter is implemented.
  - Keep the qintopia-tools Hermes registration shell thin until profile bundles can
    load skill packages directly.

id: mcp/context-server
type: mcp
name: Context MCP Server
owner: TBD
status: adopting
risk_level: high
description: MCP surface for safe Agent answer context, knowledge, and evidence routing.
source:
  path: ../qintopia-message-sidecar/src/context_mcp_server.rs
  reference: eda2652f21999e4f32699463413372accbd3b76e
  disposition: adopt
production_boundary:
  external_sends: false
  database_writes: false
  runtime_profile: false
  secrets: true
validation:
  commands:
    - cd ../qintopia-message-sidecar && cargo test
    - cd ../qintopia-message-sidecar && scripts/context-acceptance-smoke.sh
dependencies:
  - runtime/postgres
tags:
  - mcp
  - context
  - knowledge
  - evidence
next_actions:
  - Import MCP source and operations doc after runtime package split is accepted.
  - Preserve live-ops refusal guardrails in fixture coverage.

id: mcp/feishu
type: mcp
name: Feishu MCP Adapter Contract
owner: PatrickLiveCool
status: draft
risk_level: high
description:
  Feishu Base, Doc, approval, and workbench adapter contract, including the fixed
  Huabaosi generated-image version mirror schema.
source:
  path: skills/feishu-base
  reference: current-monorepo
  disposition: template
production_boundary:
  external_sends: true
  database_writes: true
  runtime_profile: false
  secrets: true
validation:
  commands:
    - pnpm mcp:adapters:check
dependencies:
  - skills/feishu-base
tags:
  - mcp
  - feishu
  - workbench
next_actions:
  - Validate the Huabaosi generated-image table schema and write permissions in an
    isolated owner-approved Base before production enablement.
  - Keep Base, Doc, and approval capabilities split behind independent write allowlists.

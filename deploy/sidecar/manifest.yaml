id: deploy/sidecar
type: deploy
name: Sidecar Deployment
owner: TBD
status: adopting
risk_level: high
description:
  Git-based rollout, smoke, and rollback contract for Agent OS sidecar services.
source:
  path: deploy/sidecar/scripts/server-deploy.sh
  reference: eda2652f21999e4f32699463413372accbd3b76e
  disposition: template
production_boundary:
  external_sends: false
  database_writes: true
  runtime_profile: true
  secrets: true
validation:
  commands:
    - cargo test --manifest-path runtime/sidecar/Cargo.toml
    - pnpm smoke:sidecar
dependencies:
  - runtime/sidecar
  - runtime/postgres
tags:
  - deploy
  - systemd
  - smoke
  - rollback
next_actions:
  - Add monorepo release gate that deploys only reviewed commit SHAs.

id: deploy/sidecar
type: deploy
name: Sidecar Deployment
owner: TBD
status: active
risk_level: high
description: Legacy deploy snapshot and cutover planning for Agent OS sidecar services.
source:
  path: deploy/sidecar/scripts/server-deploy.sh
  reference: eda2652f21999e4f32699463413372accbd3b76e
  disposition: template
production_boundary:
  external_sends: false
  database_writes: true
  runtime_profile: true
  secrets: true
validation:
  commands:
    - pnpm check
    - pnpm deploy:preflight
    - pnpm deploy:systemd:check
    - pnpm artifact:sidecar
    - cargo test --manifest-path runtime/sidecar/Cargo.toml
    - pnpm smoke:sidecar
dependencies:
  - runtime/sidecar
  - runtime/postgres
tags:
  - deploy
  - legacy-snapshot
  - systemd
  - smoke
  - rollback
next_actions:
  - During M9, use the rendered systemd review files as the target unit contract and
    copy only owner-approved units into /etc/systemd/system.
  - Use the CI-built sidecar artifact and verify artifact-manifest.json plus SHA256SUMS
    before any approved production cutover.
